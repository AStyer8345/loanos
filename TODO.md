# LoanOS — Open Work

> Granular action items: `tasks/ADAM-TODO.md`Security tracker: `tasks/security-hardening-critical-gaps.md`

## Now (this week)

### Social — BLOCKER-BRAND-003: retired entity in the draft pool (2026-08-19) 🚨 NEW

- [ ] **NEEDS ADAM — pick one: (a) rebrand the 34, (b) reject them, (c) hold until the new company name locks.** `social_drafts` holds **137 of 238 rows containing "Mortgage Solutions LP" and 0 containing "HyperSmart"**. **34 are live**: 33 scheduled drafts **2026-09-23 → 2027-02-02** (li 16 / ig 9 / fb 8) plus approved row `bbdcb13d-76e0-4fde-98ee-85739b43437e`. **Sep 23 is the deadline.** Do **not** bulk-PATCH before Adam picks — live approve-to-publish path, and GOALS still lists the new company name as pending.
- [ ] **Deal with the approved Iran row first regardless of (a)/(b)/(c).** `bbdcb13d…` is `status: approved`, `scheduled_for: null`, created 2026-03-29, opens "Rates dropped hard this week" and closes on the retired entity — a five-month-old market take that reads as current. Stale-data risk on top of the brand defect.
- [ ] **Two rate drafts carry no NMLS #513013** — Post 161 (Instagram, Oct 7, rate locks + `DM me LOCK` CTA) and Post 162 (LinkedIn, Oct 9). Explicit STEP 7 escalation trigger. Fold into whichever option Adam picks. Three other keyword matches were hand-checked and are personal posts — do not re-flag them.
- [ ] **Gate — re-run the count every session until resolved.** If `status=approved` rises above 1, or any "Mortgage Solutions LP" row reaches `posted`, escalate immediately: that means drafts are being approved in the dashboard while this is open.
- Method rule now in DECISIONS: a content-string fix is not done until **both** a repo grep **and** a `count=exact` DB query are clean. The 07-22 repo-only test is what hid this for a month.

### Social — `styer-social-am` silent run (WATCH, 2026-08-18) ✅ CLOSED 2026-08-19

- [x] ~~Confirm whether `styer-social-am` fired on 08-19 / 08-20 and left a `session-log.md` entry~~ — **CLOSED 2026-08-19.** It fired (`lastRunAt` 2026-08-19T15:00:26Z) and produced a full artifact set. One clean run closes the watch per the gate; 08-18 stays a single data point. **No blocker opened, not diagnosed.**
- [x] ~~Verify the skipped AM slot~~ — **CLOSED 2026-08-19.** The 08-19 slot was **not** skipped; it fired ~7.5h late (10:00 CDT vs 02:28) on the serialized catch-up queue that shifted every task this week. `nextRunAt` now reads 2026-08-20T07:28:25Z, the normal next slot. Not a second symptom.
- Ruled out already — do not re-check: `styer-social-am/SKILL.md` does **not** contain the "Self-Improvement — Read Before Writing" block that bounds BLOCKER-GBP-002, so that localization does not transfer. Untested candidate only: master-agent Step 1 reads `session-log.md`, now **~640 KB**.

### Social — BLOCKER-GBP-002 gate (2026-08-23) 🔴

- [ ] **PM 08-23 owns the check** of the Sun 2026-08-23 ~09:08 CDT `styer-gbp-weekly` fire (AM fires 02:28, structurally ~6.5h early). Check all three channels — `run-logs/gbp-posts/2026-08-23.md`, `Styer_Growth_Log.md`, Publer published-posts API — and record the actual result either way. If a post lands, finally live-verify the 07-22 old-brand footer fix.
- [ ] **Adam-gated:** apply the fix only if Adam has said "fix the GBP task" (ADAM-TODO L226 still `[ ]`) — bound the `learnings.md` read to ~200 lines, drop the dead `notebook_advisor.py` call from both GBP SKILL files. Live auto-publish path, so it is his call.
- [ ] **Reconcile the Publer baseline BEFORE calling the gate (new 2026-08-19).** Sessions since 07-20 record newest publish = **2026-08-06 `source: sync`** / newest `source: schedule` = **2026-07-15**. On 08-19, `GET /api/v1/posts?state=published&page=1..8` (15/page, strictly descending) topped out at **2026-06-08** for both — 07-15 and 08-06 are absent from that listing entirely. Either the standing figures came from a different endpoint/params, or the baseline is wrong. **PM 08-23 must record the exact query string it uses** — the gate is read against this baseline, so an unreconciled discrepancy can misread it in either direction. Breach ledger left at **5**, not revised on one non-reproducing query.
- Do **not** re-open the three evidence-killed suspects (GOALS pause-list collision, NotebookLM CLI hang, `learnings.md` size cliff).


### Lead Gen — execution-capacity guard (BLOCKER-008, 2026-08-17) 🔴 NEW

- [ ] **NEEDS ADAM — call Aurinder Chohan, 317-999-1866** (aurinder.s.chohan@gmail.com). Submitted 07-29 3:14 PM CDT, **never entered LoanOS**: asset depletion using $580k liquid stock, construction-to-perm ≈$1.1M. 19 days cold. Check the Jul 29 Netlify Forms notification in mail first — that backstop fired even though n8n did not. See `tasks/ADAM-TODO.md`.
- [ ] **NEEDS ADAM — authorize the burn fix** ("slow the Rancho poller"). `Ky5foSIFqMmYuny0` polls Gmail **every 5 min = 288 exec/day**, for a paused-outreach property. **↻ Re-measured 2026-08-19: it is 912 of 1,250 retained executions = 73%** of a **401/day** burn (08-17=397, 08-18=405); workflow still "Every 5 Minutes", `updatedAt` still 2026-04-25. 5min → 30min drops total burn to **~161/day ⇒ ~69 days of headroom** — permanently clear, not merely past August. Tradeoff: Rancho wedding-inquiry latency 5 → 30 min. Alternatives: raise the n8n plan, or move Rancho to Gmail push instead of polling. **While he is in there:** `QYxXYLx5WgKI9393` "Rancho Moonrise — GBP Event Sync" errors **daily at 17:00Z** (`ENOTFOUND` at `Download Image`) — trivial burn, but a second Rancho workflow failing silently every day.
- [ ] Ship via **REST PUT** once authorized (never MCP `update_workflow` — wipes credentials), then QA re-measures burn over 48h.
- [ ] **Without the fix, lead capture goes dark again ~2026-08-28 (±2d) → Sep 1.** ↻ 2026-08-19: forecast moved off the flat-10k assumption by back-solving the allowance from the one hard observation — the 07-29 refusal landed at **day 28.7 ≈ 11,150 executions** — then applying 401/day from an Aug 1 reset. The public API exposes neither quota nor cycle boundary, so **the action-by date stays 2026-08-25**, the conservative end. Instance verified healthy 08-19 15:00Z; zero `Execution limit reached` in the retained window (back to 08-16T11:00Z only).
- [x] Add a burn check to every lead-gen verify pass — **done 2026-08-19**: page `/api/v1/executions` to the end of retention, tally by day and by workflow, and read the OUTPUT of every non-success. Trigger-level refusal is invisible to every downstream check, so it must be monitored upstream.
- [ ] **Extend the standing method rule: read Supabase through service role, not the anon key.** 2026-08-19 found `GET /rest/v1/activity_log?action=eq.web_lead_created` returns `[]` / `count 0` under anon while service role returns 15 rows — RLS is no longer off on that table, contrary to `Claude/memory/tools/n8n.md` (corrected). A session trusting the anon read would conclude *zero web leads ever logged* and re-file the closed half of BLOCKER-007. **An empty anon result is not evidence of absence.**

### Lead Gen — hot-lead routing (BLOCKER-007, 2026-07-22)

- [x] Fix `Log to Activity (Web Lead)` — web-lead activity rows now write, linked to the contact (n8n `PiuIsQpBuydtFM4m`, REST PUT, QA-proven 2026-07-22)
- [ ] **NEEDS ADAM** — answer `tasks/lead-gen/specs/2026-07-22-lead-scoring-model-v2.md` §6: should a specialty-product first-touch lead fire a hot-lead email on top of the standard alert? Recommend yes. Nothing else in this section can ship until this is answered.
- [ ] Build scoring model v2 (spec §3 + §4; add §4a only if Adam approves the email). Score the lead's stated scenario, not just interaction counts. QA with `scratchpad/qa-probe.mjs`. **↻ 2026-08-14 (loanos-autonomous) — the spec's argument is now PROVEN on live traffic, not inferred.** **10 real web leads** arrived 2026-07-24 → 2026-08-13. Live scores: **nine at 6, one at 9 — all ten `cold`, zero `hot`.** The scorer fired successfully on all ten (n8n execs 40838 / 41189 / 42068 / 42144 / 44074 / 44782 / 45066 / 48429 / 48496 / 48673, all `success`), so this is **not** a plumbing failure — the model simply cannot reach 20 from a first touch. `Notify Adam` has now gone **3 weeks and 10 leads without firing once**. One of those ten came in at stage `Pre-Approved` with a 776-char scenario note and still scored 6. Against GOALS' North Star ("any inbound lead gets a call same business day"), this is the single highest-cost open defect in the stack.
- [x] ~~Add `lead_tier` to the scorer's PATCH body — it is never written today, which is why every contact reads `cold` regardless of score~~ — **CLOSED AS MISDIAGNOSED 2026-07-22 PM (loanos-autonomous), verified against live Supabase.** `lead_tier` **is** written by the scorer. Live distribution: `new` 2,993 (score 0 — never scored at all), `cold` 21 (score 3), `hot` 1 (score 70 — Emily Christensen, ADAM-TODO L20). A `hot` tier has demonstrably been written, so the PATCH body is not the defect and this edit would have changed nothing. **The real defect is the scoring model, not the tier write:** every scored lead lands at 3/100, so `cold` is the *correct* derivation from a broken score — precisely what `specs/2026-07-22-lead-scoring-model-v2.md` argues. Folded into the v2 build; no separate fix needed. (Filed from node-config inference on 07-22 AM; corrected by reading live state, per the DECISIONS 2026-07-22 standing rule.)
- [ ] Fix the scorer trigger race — `web-lead/route.ts:313` fires the scorer at contact-create, before n8n writes the activity row (spec §4a). **🟡 DOWNGRADED — the race is real in the code but has NOT fired once on live traffic; measured 2026-08-19 (loanos-autonomous).** All **11** contacts with a `web_lead_created` row scored **≥6** (ten at 6, one at 9), and 6 is precisely the score that *includes* the +3 for that row — so the activity row was counted **11/11 times**. Timing explains it: the row lands **2.3–3.9s** after the contact insert, and the n8n scorer consistently runs after that (08-18 Lee Slobodien: writer exec 50543, scorer 50545 1.2s later, both `success`). Fixing this now would be speculative hardening against a failure with zero observed instances, and it touches the same file as the unmerged `fix/web-lead-dup-23505`. **Recommend folding it into the v2 build rather than shipping alone** — v2 has to touch the trigger anyway. **One genuine finding while measuring, which sharpens the dedupe item below:** contact `982b5e94` has **3** `web_lead_created` rows but scored **6**, while `83577d99` has 3 rows and scored **9** — so repeat rows do *not* reliably stack; the scorer fires once, at first submit, and whether the extra rows count is itself a race. The dedupe rule is still right, but "+3 each, always" is not what the live data shows.
- [ ] **v2 must match the DEAL SHAPE, not just the product noun** (new 2026-08-19). Lee Slobodien (08-18, 6/cold) described a **$250–400k Hill Country investment property run as a short-term rental**, 20–25% down, strong W-2, and asked for a call before applying — and never wrote the word "DSCR" or any other noun on §6's specialty list. A product-name keyword matcher scores him 0 on the specialty signal. **Borrowers describe their situation; only lenders name the product.** Add investment property / short-term rental / rental / second home to the match set.
- [ ] **v2 must DEDUPE `web_lead_created` per contact, not count it** (new 2026-08-17). Repeat rows stack additively at +3 each, so the 07-28 triple-submit **bot** (`plTNFdDvbCclmsydWGO`, 3 rows) scored **9** — higher than every genuine single-submit prospect at 6. Until Defect 2 is fixed the double/triple-submit race is a scoring exploit that ranks bots above borrowers, and raising the threshold alone would not fix it.
- [ ] Re-score Simrann Sidhu + Thane Kwon once v2 is live — both specialty files. **↻ 2026-08-14: the baseline moved.** Scored leads no longer land flat at 3 — the Defect 1 fix added the missing activity row, so first-touch leads now score **6** (9 with an extra interaction). Still nowhere near the hot threshold of 20; the ceiling problem is unchanged.
- [x] ~~Verify Defect 1 on a real live lead~~ — **CLOSED / VERIFIED 2026-08-14 (loanos-autonomous), live Supabase.** `activity_log` now holds **14 `web_lead_created` rows, 14/14 with a non-null `contact_id`**, spanning **2026-07-24 21:22 UTC → 2026-08-13 19:07 UTC**. The first row landed 2026-07-24 — ~2 days after the 07-22 15:23 REST PUT, exactly the arrival window the 07-22 note predicted. Zero rows existed before the fix, so every one of these is post-fix. The n8n side corroborates: scorer `nOCDV73m4M0jyL1B` shows **10 webhook executions, all `success`**, timestamps matching the 10 web-lead contacts one-for-one. Plumbing is proven end-to-end on real traffic. Confirmed, not assumed.
- [x] ~~Defect 2 (app code, needs build + push + Vercel verify) — `src/app/api/contacts/web-lead/route.ts:263`: catch Postgres `23505`, re-select by email + org, return existing contact with 200. Kills the double-submit 500s and the duplicate alert emails.~~ — **CODE SHIPPED 2026-08-17 (loanos-autonomous), commit `26d2601` on branch `fix/web-lead-dup-23505`, Vercel preview `dpl_7dCAJDeA2vs1ebgcbfF4PKR6uBd1`. NEEDS ADAM: merge the PR — autonomous runs cannot merge to `main`, so this is not in production yet.** The insert now catches `23505`, re-selects by `organization_id` + `ilike(email)`, and returns the existing contact in the same `{contact, duplicate, message}` shape § 4 already returns — so n8n consumers need no change. **Constraint verified live before writing the fix:** the index is `contacts_email_org_unique ON (email, organization_id)` — a plain case-sensitive btree, while the § 4 pre-check uses `ilike`. That asymmetry is why the race exists and why no pre-SELECT can close it; only the DB constraint arbitrates, so catching the error code is the sole correct fix. **How this shipped after a 30-day push deadlock:** the exit the 07-22 run identified was used for the first time — `git worktree` off `origin/main` (`fix/web-lead-dup-23505`), which sidesteps the diverged `codex/website-ai-assistant` clone entirely. `route.ts` was re-verified byte-identical between `HEAD` and `origin/main` first, so the fix applies cleanly to either. Build green first pass, lint clean, pre-push hook passed. **The worktree exit is now proven, not theoretical — future autonomous code work should use it rather than reporting the clone as blocked.**


### Unified Command Center (Adam-directed 2026-07-02 — Phase 1 SHIPPED, follow-ups below)

- \[x\] ~~Status normalization (`loans.status_normalized` + trigger + backfill), comp tracking (`comp_plans` + `loan_compensation` + auto-create on funded), dashboard widgets (NotesScratchpad / UnknownSenders / Stalled), CompensationPanel on Performance tab, Inbox Review create-contact action~~ — DONE 2026-07-02, see CHANGELOG.
- \[ \] **Adam: sanity-check comp plan defaults** on Performance tab → Compensation → Plan defaults. Seeded from the Kyber spreadsheet (200 bps / 10% share / 25 bps LOA / $879 + $379 fees). Especially: LOA fee applies to EVERY loan at 25 bps — zero it if most files have no LOA, then correct individual rows inline.
- \[ \] **Adam: triage the unmatched-email backlog** — 788 open unmatched inbound emails. The dashboard widget + Inbox Review now have create-contact and ignore-sender actions; a focused 20-min pass on the top senders clears most of it.
- \[ \] **Stamp `lead_source` onto loans at creation** — loan-level source is 63/1,313 populated; heals conversion-by-source over time.
  - \[x\] ~~MISMO importer (`/api/mismo/import`) stamps the linked contact's `lead_source` onto the loan~~ — DONE 2026-07-03 loanos-autonomous (Scott's pilot path). Build green.
  - \[ \] **Arive webhook path (primary, ~1,250 loans) — NEEDS ADAM/supervised.** n8n `1tagvoU0UXtdDiMY` (prod loan-sync write path) doesn't stamp `lead_source`; add contact lookup + copy onto loan insert via REST PUT (credential-preserving). See tasks/ADAM-TODO.md.
  - CSV importer (`/api/import/loans`) does not link loans to contacts — no source to copy (would need a "Lead Source" column mapping instead; out of scope for this item).
- \[ \] **Waiting-on classification** for needs-attention: me vs borrower/processor/title, ranked by wait time. Heuristics off last activity_log entry first; AI classification (existing inbound-email Claude pattern, model `claude-fable-5`) later. **🟡 BLOCKED ON A MISSING PREREQUISITE — measured 2026-08-19 (loanos-autonomous), do NOT build as specified.** The prescribed heuristic reads "off last activity_log entry", but **there is no outbound side to read.** Live counts: `activity_log.email_outbound` = **3 rows, newest 2026-03-31**; `contact_emails` = **10 rows, all outbound automation seeds, newest 2026-03-15**; `todo_items` = **60 rows, all `source: manual`, newest 2026-04-14**. Decisive check: across **17 of 17** widget-eligible inbound emails in the last 60 days, **zero** have any outbound activity logged after them — the only later rows are `system` and further `email_inbound`. So "who are we waiting on" resolves to **"you" on 100% of rows**: a constant badge, not a classification, and shipping it would make the widget look smarter while telling Adam nothing. The role half is no better — `contacts.contact_type` holds only `borrower` (1,727) / `realtor` (1,195) / `other` (126); **there is no `processor` or `title` value anywhere**, so three of the four categories in this line's own title are underivable. **Real prerequisite, and it is the actual work: log outbound email.** Nothing records Adam's replies today, which also means "ranked by wait time" is measuring time-since-they-wrote, not time-we-owe-them. Sequence: (1) write outbound sends to `activity_log` (or revive `contact_emails.direction`), (2) let a week of real traffic accumulate, (3) then the heuristic has two sides to compare and this item becomes buildable in an afternoon. Filed to `tasks/ADAM-TODO.md`.
- \[x\] ~~**Duplicate scan v2**: add phone + fuzzy-name matching to `/api/contacts/duplicates` + dashboard nudge when candidates exist (email-exact already live; only 5 dup pairs today).~~ — **CODE SHIPPED 2026-08-18 PM (loanos-autonomous), commit `0c130d8` on `feat/duplicate-scan-v2`, Vercel preview `dpl_EdCtsAtUbmC9dm8P68xYmqPHDnpm` READY. NEEDS ADAM: merge the PR — autonomous runs cannot merge to `main`, so this is not in production yet.** New match types `phone` (identical 10-digit national number) and `fuzzy_name` (same surname + first-name variant) computed in the route; RPC left untouched. Pure logic in `src/lib/contacts/duplicateMatch.ts` with 14 unit tests. **Both thresholds were measured against the live book (2,466 contacts), and measuring changed the design twice:** last name + first initial returns **72** groups of mostly different people (Jennifer/Jerry Brooks) vs **4** for a real first-name variant; a bare shared-phone rule returns **30** of which **11 are plainly spouses/co-borrowers/office lines**, so phone matching is gated on a corroborating name signal → **19**. Precision is deliberate: a missed candidate leaves a duplicate in the CRM, a false candidate invites a merge that destroys a real record (this is also why `jon`/`john` is knowingly missed — a 3-char edit floor would pair `jim`/`tim`). **Verified two independent ways:** the SQL probe and the shipped TypeScript agree exactly (19 phone / 4 fuzzy, same keys), and the 2,466-row count proves the new pagination loop — an unbounded PostgREST select silently caps at 1,000 and would read as "no duplicates" past that row. **⚠️ Deviation, deliberate:** the nudge is a live candidate count on `FIND DUPES` at `/dashboard/contacts` (via new `?countOnly=1`), **not** a widget on the main dashboard — `DashboardClient.tsx` differs between `origin/main` and Adam's in-flight codex branch, so editing it would hand him a merge conflict in the dashboard he is actively rewriting. Main-dashboard placement queued in `tasks/ADAM-TODO.md` if he wants it.
- \[ \] **Lead-source label cleanup**: 428 recent contacts say `point-import` (migration artifact); normalize casing variants (`Web Lead`/`web lead`/`Website`).

- \[x\] **~~Lead Gen — BLOCKER-006 lead scoring/routing down~~** RESOLVED 2026-06-09 AM (lead-gen-am). Two bugs fixed via n8n REST PUT: webhook `responseMode`→`onReceived` + rebuilt stale Supabase cred (`qjRCjm5wKJgPGXXY`→`Bi7VTMWZeMnTrS3h`). QA exec 24136 success. Speed-to-lead live again. **Open follow-up → ADAM:** backfill 25 unscored leads since 05-01 (re-POST re-fires hot-lead alerts, so Adam runs it). Detail: BLOCKERS.md BLOCKER-006 (RESOLVED).

> **2026-05-18 lead-gen-am triage:** Refreshed GOALS (Week of 2026-05-18) re-ranks the lead-gen pile. Full triage memo: `tasks/lead-gen/audits/2026-05-18-pile-realignment.md`. New ranked Adam short-list replaces the flat 5+ item PR pile below — original lines kept for traceability but **flagged with triage verdict in line 1**. New short-list order: (1) Realtor Relationships Phase-1 3 decisions [KEEP], (2) Calendly HMAC signing-key paste [KEEP — freebie], (3) Phase A compliance bundle (reconciles PR-1 + PR-4 + parts of PR-5/Homepage-Forms-TCPA with 2026-05-17 site audit) [KEEP], (4) Past Client Retention drip copy direction [KEEP — Tier 1 outbound priority], (5) `notebooklm login` [KEEP — 17-day backlog]. **Tier 4 RETIRE/CLOSE:** PR-2 Conversion Consolidation (superseded by 2026-05-17 site audit "scenario review" CTA architecture); 2026-04-27 Realtor Relationships cadence question (superseded by 05-14 spec); 2026-04-27 CRON_SECRET (LoanOS paused by GOALS). **Tier 2 RECONCILE / Tier 3 DEFER:** see triage memo § "Pile Inventory + Verdicts".

- \[ \] **[TIER 1 KEEP — top of new short-list]** **Lead Gen — Realtor Relationships activation spec ready** (2026-05-14 AM): Architect-mode session re-frames the open 2026-04-27 ADAM-TODO #2 cadence/activation question in light of the actual trigger pipeline implementation gap. Spec at `tasks/lead-gen/specs/2026-05-14-realtor-relationships-activation-spec.md` (~357 lines). **Discovered the real blocker:** `src/app/api/drip/run/route.ts` lines 119-129 only compute `next_send_at` from `relative_days` triggers. `annual_date` + `condition` trigger types have **no evaluator anywhere in the codebase** — Steps 1, 2, 4 (as currently configured in `drip_steps`) cannot fire on schedule today regardless of cadence/activation decisions. **Phase-1 plan** (~60 min Builder, ship-now): restructure all 4 steps to `relative_days` (0/90/180/270 day cadence — quarterly touchpoints aligning with campaign description "3-4 touchpoints/year"); Pool B batch enrollment (158 distinct realtors with at least 1 linked closed loan via `buyer_agent_contact_id`); merge-tag resolver extension for `transaction_address`/`transaction_buyer_name`/`deal_count`/`first_deal_date` via JOIN on `loans` keyed by `buyer_agent_contact_id`; email body subject/intro adjustments for Steps 1+4 (drop "one year ago" + "Thanksgiving" framing, replace with deal-specific anchor + year-end check-in); `drip_steps` UPDATE migration (3 statements); pre-filter on `email_opt_out=false` + 90-day bounce/complaint history. **Phase-2 plan** (~3 hrs Builder, deferred): build `annual_date` + `condition` trigger evaluators if Phase-1 engagement justifies (≥30% open + ≥3% reply across first 50 sends). § 5 has **3 Adam decisions (~5 min total)** each with sensible defaults: Pool B full (158) vs Pool B-recent (last 24 months, ~50-80); auto-send vs queue for review; copy reanchor as proposed. **Default recommendation: ship Phase-1 with all 3 defaults.** Risk: 7-row assessment, all LOW or MEDIUM with mitigations (M2 = empty-merge-tag skip rule, M7 = manual hold-list pre-filter). 12-step Builder execution checklist included. Live state read-only at session start: drip_enrollments_total=0, drip_sends_total=0 org-wide → cleanest possible greenfield. Once Phase-1 ships, agent flips the 04-27 ADAM-TODO #2 (cadence/activation reference) line + this 05-14 line to `[x]` in same session. Forward-rule pivots tomorrow's session to Long-Term Nurture authoring OR Past Client Retention authoring (both 04-27 NEEDS ADAM open lines).
- \[ \] **[TIER 2 RECONCILE — cherry-pick into Phase A bundle]** **Lead Gen — PR-5 Final Light-Pass spec ready** (2026-05-10 AM): 60-min Builder ship CLOSES THE ENTIRE 4-AUDIT PILE. ~40 atomic edits across 6 conceptual clusters spanning 6 source files (`get-preapproved.html`, `rate-alert.html`, `index.html`, `thank-you.html`, `script.js`, `subscribe-lead.js`). Cross-cut A (Loan Goal taxonomy unified across 3 funnel surfaces + TAG_MAP); Cross-cut B+C (MortgageBroker + Service JSON-LD + meta description + OG image fallback on get-preapproved + rate-alert); Cross-cut D (21-day footnote sourcing); homepage polish + M2 purchase_price_range cross-page parity (mirror PR-2 pattern); get-preapproved hero promotion + microcopy; thank-you + rate-alert polish bundle (per-branch document.title, per-branch Calendly h2, em-dash literal cleanup, GA sessionStorage dedup, mobile order removal, form-tagline microcopy, rate-move language softened, loans-closed fallback social proof). 8 LOW + 2 NONE risk rows. § 6 Adam-data prereqs (3 decisions, ~3 min): canonical Loan Goal taxonomy variant; canonical email + address (carry-over from PR-4 § 6); 21-day average confirmation. Spec at `tasks/lead-gen/specs/2026-05-10-final-light-pass-pr-spec.md`. ADAM-TODO line filed. Sequencing: designed to apply LAST after PR-1 → PR-2 → PR-3 → PR-4 (PR-3+PR-4+PR-5 can bundle in one Builder push). PR-5 must rebase on top of PR-2 for `script.js` Quick Contact handler conflict, `index.html` line 408 CTA, and `get-preapproved.html` lines 376–382 Loan Goal. Once shipped, Builder flips ALL 4 prior audit ADAM-TODO lines + ALL 4 prior PR lines + this PR-5 line to `[x]` — audit-series queue fully drained.
- \[ \] **[TIER 1 KEEP — merge into Phase A bundle]** **Lead Gen — PR-4 Cross-Page Brand-Consistency + Footer-Address spec ready** (2026-05-09 AM): 30-min Builder ship closes the cross-page consistency debt that PR-1 / PR-2 / PR-3 explicitly deferred. 4 funnel pages, 6 atomic copy-paste-ready diffs: get-preapproved M5 (footer physical-address gap — Texas SAFE Act / NMLS MU.4); 6 surviving `thestyerteam.com` brand-rule violations across `index.html` JSON-LD × 2 + footer × 1 + `rate-alert.html` footer × 1 (PR-1 only swaps thank-you line 717, PR-2 only swaps rate-alert line 460); thank-you M6 Google Ads conversion suppression for `?type=lo-waitlist` (LoanOS waitlist signups inflate mortgage funnel ROAS). All 7 risk rows LOW or NONE. Spec at `tasks/lead-gen/specs/2026-05-09-cross-page-brand-footer-pr-spec.md`. ADAM-TODO line filed. § 6 Adam-data prereq: canonical address (production = 5718 Sam Houston Circle on 5+ files, compliance docs reference 5900 Balcones Drive — 30-sec decision before Builder ships). Independence: PR-4 touches different line ranges from PR-1/PR-2/PR-3 in every shared file (sequencing matrix in spec § 10) — can ship in any order. Recommended last (lowest priority brand polish). After PR-4, only PR-5 light-pass M+L tier remains in the audit-series queue.
- \[ \] **[TIER 2 RECONCILE — defer until positioning copy locks]** **Lead Gen — PR-3 Thank-You Conversion spec ready** (2026-05-08 AM): 25-min Builder ship completes the consolidation trilogy (PR-1 compliance + PR-2 form-page + PR-3 post-submit). Single file (`thank-you.html` IIFE only, lines 621–720); 4 atomic copy-paste-ready diffs (rate-alert keep + retitle Calendly, FTB-DPA append phone CTA, PA-branch reassurance copy, unknown/no-type dataLayer instrumentation). All 6 risk rows LOW or NONE — cleanest of the trilogy from a risk standpoint. Spec at `tasks/lead-gen/specs/2026-05-08-thank-you-conversion-pr-spec.md`. ADAM-TODO line filed. Independence: zero overlap with PR-1's thank-you edits or PR-2's form-page files; can ship in any order. Once shipped (combined with PR-1), the 05-05 thank-you-page audit ADAM-TODO line gets marked `[x]`.
- \[ \] **[TIER 4 RETIRE — superseded by 2026-05-17 site audit "scenario review" CTA architecture; recommend CLOSE]** **Lead Gen — PR-2 Conversion Consolidation spec ready** (2026-05-07 AM): 45-min Builder ship bundles H2–H5 conversion-focused HIGH-tier findings from 3 form-page audits (05-01 / 05-02 / 05-04) into one PR — 8 atomic copy-paste-ready diffs across `get-preapproved.html`, `rate-alert.html`, `index.html`, `script.js` + inline get-preapproved handler. 3 findings split into "PR-2b deferred" with copy-paste templates ready (need Adam-supplied GBP `place_id`, real testimonial names, subscriber count). Spec at `tasks/lead-gen/specs/2026-05-07-conversion-consolidation-pr-spec.md`. Sequencing: PR-1-then-PR-2 preferred (closeout ships first), but PR-2 also safe alone. ADAM-TODO line filed. Once shipped, the 3 prior audit lines (05-01 / 05-02 / 05-04) get marked `[x]`.
- \[ \] **[TIER 1 KEEP — merge into Phase A bundle]** **Lead Gen — Compliance Closeout PR spec ready** (2026-05-06 AM): single 30-min Builder ship resolves BLOCKER-001 + 4 of 5 series compliance FAILs across the 4 funnel-page audits. Spec at `tasks/lead-gen/specs/2026-05-06-compliance-closeout-pr-spec.md`. ADAM-TODO line filed. Awaits Adam authorize. Once shipped, the 4 prior audit lines (05-01 / 05-02 / 05-04 / 05-05) get marked `[x]`.
- \[x\] ~~Adam: fill in~~ `extractPayloadIdentity()` ~~in~~ `src/lib/los/verifyLosPayload.ts` — DONE. Implemented with `loanOfficerEmail` (confirmed 2026-04-04 Zap run). Verified 2026-04-09.
- \[x\] Apply migration 075 (`los_integrations`) to Supabase (done 2026-04-08)
- \[x\] ~~Run PII backfill script~~ — DONE (1403/1403 companions). Migration 083 applied 2026-04-12: plaintext columns dropped.
- \[x\] ~~Adam: register FRED API key~~ — NOT NEEDED. Sequence A uses Set Rate webhook (Option A). Resolved 2026-04-09.
- \[x\] ~~Adam: verify Microsoft Outlook credential in n8n UI~~ — DONE 2026-04-10 AM. Sequence A + Anniversary Check-In both ACTIVE (verified via MCP).
- \[ \] **NEEDS ADAM — `loanos-launch-standup` scheduled task fired against paused goals (2026-05-23 AM — 6th consecutive AM no-op; full-week-of-no-ops milestone hits AM 05-24 next; AM 05-22 was 5th and forward-warned this fire).** GOALS.md Week-of-May-18 "Pause List" line 56 explicitly names `loanos-launch-standup` as a task to pause; "No LoanOS product work — paused indefinitely." (GOALS.md re-read this session — mtime carries from Sun 2026-05-17 12:11 CDT, Week-of-May-18 directive still governs; next refresh window = Mon 2026-05-25, 2 days out). Today's standup cron fired anyway (current date 2026-05-23, launch target was 2026-04-26 — now 27 days past launch). Standup task config still expects pre-launch posture ("Days to launch: N", launch-plan focus, audit findings surfacing). Per scheduled-task wrapper rule ("If your task conflicts with current goals, log the conflict to your project TODO.md under NEEDS ADAM and stop"), this AM's run again produced no standup output — `tasks/standup-log.md` not appended, no Vercel/n8n queries run, no audit re-surfacing, no CHANGELOG entry. Refreshed-in-place pattern preserved (6th touch), no new ADAM-TODO line stacked. **Pick one: (a)** disable the `loanos-launch-standup` scheduled task entirely (program complete, +27 days post-launch, GOALS pauses it through indefinite horizon — **strongest recommendation, ESCALATING — 6-day streak crossing into full week of cron no-ops tomorrow; recommend Adam decide at Mon 05-25 refresh regardless of LoanOS direction. Standup format ("days to launch") is structurally broken post-launch, so no reasonable un-pause restores it as-shipped**); **(b)** repurpose the cron slot to a non-LoanOS active task per current GOALS (styermortgage.com compliance cleanup or lead-gen pipeline); **(c)** leave dormant — accept daily no-op cron fires + this NEEDS ADAM entry refresh until GOALS lifts the pause. Forward warning: AM 05-24 = 7th consecutive no-op (full week of cron no-ops); AM 05-25 = 8th and coincides with GOALS refresh window (Adam's natural decision point); AM 05-26 = 9th if GOALS refresh defers the decision. Every additional AM fire = one more no-op + one more TODO touch with no value produced.
- \[ \] **NEEDS ADAM — Resend DKIM verification for Scott's sender domain.** Per-org From: address + Reply-To shipped 2026-04-29 (commit `4ac0812`, migration 095). `org_settings.from_email` for Scott's org is set to `scott@mortgagesolutionslp.com` but `mortgagesolutionslp.com` is not yet DKIM-verified at Resend — until verified, any drip sends from Scott's org will be rejected by Resend. Adam org `thestyerteam.com` already DKIM-verified (existing transactional sends). Resolution: log into Resend dashboard → Domains → add `mortgagesolutionslp.com` → publish the SPF + 2 DKIM CNAME records on the domain → click Verify. Blocks live drip sends for Scott's pilot.
- \[ \] Adam: upload selfies → unblocks LoanOS social content stream
- \[ \] Social posts: **maintenance-only, Builder held** (positioning + site-copy lock during the MSLP→HyperSmart compliance-review transition). Positioning per GOALS Week-of-May-18: "complicated income" specialist + wholesale pricing, story-driven, NO "21-day close", NO performance-metric marketing, 1-2/week at 9/10. **Cushion: 47 drafts (SQL-authoritative — 47 draft / 2 approved / 7 posted / 179 rejected as of 06-19 PM; REST head occasionally reads 48 = known off-by-one), scheduled Sep 23 2026 → Feb 4 2027.** **GBP distribution: 7 pieces ready, awaiting one "ship it"** — 2 new 06-14 blogs (ADAM-TODO 06-15: DSCR-vs-conventional + buy-before-you-sell) + 06-08 "When Other Lenders Say No" newsletter (ADAM-TODO 06-09) + 06-15 rate/market blog "Mortgage Rates, Inflation & the Iran Peace Deal" (ADAM-TODO 06-16, **TIMELY/decaying — most urgent, ship first if partial**) + 06-16 "DSCR Loan Requirements Texas 2026" evergreen blog (ADAM-TODO 06-18) + 06-23 "No-Ratio & Low-DSCR Loans in Texas" + 06-22 "Buyers Who Don't Fit Conventional" newsletter (ADAM-TODO 06-27). No new content since 06-23 (06-30 AM live scan). Auto-publish held because Adam's 06-06 GBP OK was batch-specific; one word clears all 7 under the HyperSmart footer. **⚠️ Recurring old-brand leak:** `styer-gbp-weekly` scheduled task auto-published a fresh OLD-brand GBP post Sun 06-28 ("Week 26 Self-Employed", footer "Adam Styer | Mortgage Solutions LP | NMLS #513013") — repeats every Sunday (next 07-05) until Adam picks ADAM-TODO L12 a/b/c. **Other open-for-Adam:** cushion-footer A/B/C (33/47 old drafts + 2 stray Publer posts carry MSLP — one decision covers both); `rates/2026-05-18.html` (4 wks stale — fresh update or OK to post dated); GBP listing Google-side rename; `stat -L` symlink fix (Builder-shippable, just flip `[x]`). NotebookLM CLI auth expired. Refresh 07 no-op (earliest draft 2026-09-23). Old items still open: Post 157 blog link in first comment (BEFORE Sep 24); Canva/Reel backlog (Posts 43/44/47/51/52/55/60/81/113/115, built under old positioning — keep-vs-rewrite pairs with footer disposition). Forward rule: first action each AM = `stat -L -f "%Sm" GOALS.md` (never bare `stat -f`); break maintenance only when the footer disposition answers AND site positioning copy locks. **↻ 2026-07-20 PM — bundle is 9 GBP-ready** (adds 06-30 bank-statement + 07-16 self-employed-underwriting; ADAM-TODO L17 is authoritative). **Breach ledger corrected to 5 old-brand auto-publishes** — 06-21/06-28/07-05 GBP + **07-10 Facebook** + **07-15 GBP**; the last two never wrote `social_drafts` rows, which is why 07-15→07-18 sessions wrongly reported the streak "paused at 3." Sun 07-19 fire ran but published nothing; next 07-26. Cushion live: 48 draft (REST head) / 2 approved / **10** posted / 179 rejected. **↻ 2026-07-22 AM — old-brand footer ROOT-CAUSED AND FIXED; bundle 7→10.** The fix this TODO and ADAM-TODO L16 kept recommending — "edit the footer in `~/.claude/scheduled-tasks/styer-gbp-weekly/SKILL.md`" — **was never executable**: that file has no footer template and no old-brand string (line 92 already mandates HyperSmart), and the delegated `gbp-weekly-optimization/SKILL.md` is 100% HyperSmart. **Real source = the voice guide** `tasks/social-media/adam-voice-and-workflow.md` L10/L93 + the identical Supabase `social_settings.voice_guide` mirror, which `styer-gbp-weekly/SKILL.md` declares MANDATORY and *overriding all generic voice guidance* — so it outranked the "never use MSLP" line 52 lines below it. **Both copies fixed this session** (file + Supabase PATCH → 204, re-read verified; prior value backed up). Internal config only; no live post edited, no task paused. **NOT yet verified** — the Sun **07-26 ~09:08 CDT** fire is the test; **AM 07-27 must live-verify the footer via the Publer published-posts API** and record the result. If it still leaks, next suspect is few-shot contamination from the 8 `run-logs/gbp-posts/*.md` files that still contain the MSLP footer. **GBP-ready bundle now 10** — adds 06-30 bank-statement, 07-16 self-employed-underwriting, and 07-21 ITIN (`blog/2026-07-21-itin-mortgage-loans-texas.html`, HyperSmart ×11 / NMLS ×6 / 0 rate figures; ship it with an ECOA/FHA care note since ITIN content is immigration-status-adjacent). Still one "ship it" clears all 10; 06-15 rate/market first (now ~5 wks decayed). **New for Adam:** the same voice guide L11 calls Adam a "Correspondent lender — not a broker," contradicting CLAUDE.md's "Independent mortgage broker" and GOALS' wholesale/40+-lenders framing — not changed (positioning claim, not a brand string); ADAM-TODO L16. **↻ 2026-07-22 PM — the AM fix above was INCOMPLETE; 5 more old-brand MANDATES found and fixed.** AM corrected the voice guide and stopped. A full sweep found five more files that actively **prescribed** the retired name, none of them the voice guide: `tasks/social-media/master-agent.md:142` mandated **the exact published footer verbatim** ("ALL posts must include: 'Adam Styer | Mortgage Solutions LP | NMLS #513013' at the end") *inside the Step 1B GBP auto-publish rules*; `:37` was a CRITICAL RULE ordering it; `:20`/`:66` reinforced it; `subagents/03b-quality.md:12` anchored the quality gate to it; `subagents/03-builder.md:239` baked it into every Canva visual brief; and worst — **`subagents/04-reviewer.md:99` was an INVERTED COMPLIANCE GATE** reading `Business name: "Adam Styer | Mortgage Solutions LP" → REJECT if wrong name appears`, i.e. the one control built to catch this breach was set to **reject correctly-branded HyperSmart posts and pass old-brand ones**. All corrected to "Adam Styer | HyperSmart Home Loans"; **verified 0 prescribing lines remain** across master-agent + voice guide + all subagents (3 leftovers are "never use/REJECT" bans, the correct form). **Scope bounded:** swept the 6 sibling Styer scheduled tasks (site-daily, blog-writer-weekly, content-weekly, suburb-editor-daily, calculator-audit-weekly, gbp-weekly) — **all already correctly branded**; social was the lone unmigrated domain, which is why only social/GBP leaked. No out-of-domain edits. **Standing rule added (DECISIONS 2026-07-22 PM):** a fix is not done until a repo-wide grep shows zero prescribing instances — a green grep proves the *instructions* are clean, not the *output*. **Still unverified end-to-end:** Sun 07-26 ~09:08 CDT fire is the test; AM 07-27 live-verifies via the Publer published-posts API. **↻ 2026-08-15 PM — the verification this line has been waiting on since 07-22 STILL cannot run, and the reason is a new and bigger problem: `styer-gbp-weekly` has published nothing for 5 weeks.** The task is enabled and firing (`lastRunAt` 2026-08-10T16:14:05Z) but produced **zero artifacts across 4 consecutive fires** (07-19 / 07-26 / 08-02 / 08-09) on all three of its output channels — `run-logs/gbp-posts/` newest still `2026-07-15.md`, `Styer_Growth_Log.md` newest still 07-15, Publer newest `source: schedule` publish still 07-15. **Last live GBP post = 2026-07-15.** So the 07-22 two-layer brand fix is intact (re-greped 08-15: 0 prescribing lines repo-wide, only ban lines remain) but **unverified for want of a post to verify**, not because it failed. **Breach ledger unchanged at 5** — the only 3 publishes since 07-15 (07-31 ×2 client-testimonial post, 08-06 photo) are all `source: sync`, i.e. Adam's own manual posts, 0 MSLP / 0 "Styer Team" hits. **Scope: GBP-specific, not a scheduler outage** — `styer-blog-writer-weekly` shipped `blog/2026-08-06-asset-depletion-loan-case-study-former-cfo.html`, site commits through 08-14. **Root cause NOT determined and deliberately not guessed** (this line already carried a non-existent fix for ~12 sessions): the dead path `python3 /Users/adamstyer/loanos/scripts/notebook_advisor.py` — present in both GBP SKILL files and 4 other live tasks, home-root `loanos/` archived 04-23 — was checked and **ruled out**, since GBP published fine for 10 weeks after that archive. **No instruction files edited** (live auto-publish path firing in ~12h). **Next fire Sun 08-16 ~09:08 CDT, ISO wk 33 → theme 2 DSCR/Investor; PM 08-16 owns the check** — AM fires 02:28, ~6.5h before the post, and never could have done it (DECISIONS 2026-08-15). Cushion re-read with `count=exact`: **47 draft / 2 approved / 10 posted / 179 rejected**, drift 0. **↻ 2026-08-16 PM — the window closed and the answer is negative: the 08-16 fire ran and published nothing. 5th consecutive silent fire ⇒ confirmed persistent task-level defect.** `lastRunAt` **2026-08-16T16:09:34Z** (it did fire); zero artifacts on all three channels; Publer returns **0 posts 08-10→08-18 across all five states** (published/scheduled/draft/pending_approval/failed); newest publish of any kind still 08-06 `source: sync`. Brand fix unverifiable for a 6th session — still for want of a post. **🟢 DEFECT LOCALIZED, via a correction to the 08-15 entry above: run transcripts for the silent fires DO exist** (`local_92c3fae6` = 08-16, `local_94d95e51` = 07-19) — the "no attributable transcript" claim was false and was the sole reason root cause looked undeterminable. Searching the transcript index as a **negative oracle**: the 08-16 run **read GOALS.md + `styer-gbp-weekly/SKILL.md` + the delegated `gbp-weekly-optimization/SKILL.md`** (all confirmed) but shows **no trace of Step 1 theme determination** ("Week 33" absent from its transcript, present in 2 sibling sessions the same day), ran **~3h35m** (16:09:34Z → 19:44:53Z), and died **1.3s** before `styer-social-am` claimed the serialized runner. ⇒ Failure is inside the **"Self-Improvement — Read Before Writing"** block — the only step between the last confirmed read and the first missing output — which reads a **324 KB** `run-logs/learnings.md`, reads `run-logs/competitive/latest.md`, and calls the dead `python3 /Users/adamstyer/loanos/scripts/notebook_advisor.py`. **Three suspects ruled out with evidence (do NOT re-open):** (1) GOALS.md pause-list collision — GOALS L61 pauses `gbp-optimization` and `styer-gbp-weekly` is absent from "Keep running", so a compliant self-halt looked likely, but the run demonstrably read the delegated SKILL *after* GOALS; (2) NotebookLM CLI hang — probed live, exits in **3s** with a `WebLiteSignIn` auth-expired redirect; (3) `learnings.md` size cliff — git history shows smooth growth (285 KB 06-15 → 324 KB now), no step change at the 07-15→07-19 onset, and GBP published fine at 285 KB. **Precise cause still NOT determined and not guessed** — but bounded to ~10 lines instead of a whole task. **Fix needs Adam's OK** (live auto-publish path): bound the `learnings.md` read to ~200 lines + remove the dead advisor call from both GBP SKILL files (also in `styer-site-daily`, `styer-competitive-weekly`, `competitive-intel-weekly`). Trigger phrase filed in ADAM-TODO L226: **"fix the GBP task"**. Next fire **Sun 08-23 ~09:08 CDT** — 6 days out, so fixing now no longer contaminates the observation window; **PM 08-23 owns the check**. `BLOCKER-GBP-002` opened. Cushion unchanged: 47 / 2 / 10 / 179, drift 0. **↻ 2026-08-17 AM — bundle 10 → 11; the new piece is the best in it, and it sat un-routed for 11 days.** Step 1B found `blog/2026-08-06-asset-depletion-loan-case-study-former-cfo.html` ("No W-2, No Problem" — former CFO, sabbatical after a liquidity event, closed on asset depletion). HyperSmart ×13 / 0 MSLP / NMLS #513013 ×6 / **0 rate figures** (no APR trigger), evergreen. GOALS line 21 names "asset depletion", and it is **the only bundle piece that is a real closed file rather than a product explainer** — the format the voice guide says beats definition cards. Routed to tracker + repost queue (2 distinct native angles) + ADAM-TODO L17 in place. **🔴 Miss cause — a coverage gap, not a scan defect: no AM session ran between 07-18 and today** (07-23/08-15/08-16 were all PM; PM skips Step 1B). Guard: after PM-only runs, scan back to the last **AM** tracker entry, not the last session. **Say-it-this-way when asking Adam:** the ship-it bundle schedules through Publer directly and is **independent of BLOCKER-GBP-002** — the broken weekly task does not block it. BLOCKER-GBP-002 re-measured, no drift (`lastRunAt` 08-16T16:09:34Z, `nextRunAt` 08-23T14:08:30Z; Publer newest publish 08-06 `source: sync`, newest `source: schedule` 07-15, scheduled queue empty); **not re-diagnosed** per the blocker's own instruction. Cushion 48 REST (=47 SQL ±1) / 2 / 10 / 179, drift 0.
- \[x\] ~~**Trace the 07-10 Facebook auto-publisher**~~ — **RESOLVED 2026-07-22 AM: not a live auto-publisher, no two-tier violation.** It was the legacy pre-rename Publer-queued Facebook post `69d904b3b17de1805a6e4a87` — MSLP footer baked into its text, scheduled back in ~April — which the AM 06-08 session had **already traced and explicitly predicted would fire Jul 10 9am CT**. It fired and cleared: Publer `state=scheduled` is now **empty (0 posts)**, so zero pending old-brand posts remain. `grep -rli publer ~/.claude/scheduled-tasks/` → 5 files; `styer-gbp-weekly` + `gbp-weekly-optimization` both target `GBP_ACCOUNT` only and explicitly forbid IG/FB/LI auto-publish, `daily-briefing` + `n8n-health-check` are read-only, and `gbp-weekly-optimization` is **not a registered scheduled task at all** (absent from `list_scheduled_tasks`). No running task posts to a non-GBP Publer account. Leftover: Publer LinkedIn **draft** `69c92fa536ecd279f42a7d4b` still carries the old footer but cannot self-publish — Adam's call.
- \[ \] **Use the Publer published-posts API as the breach check every session** (2026-07-20 PM). `GET https://app.publer.com/api/v1/posts?state=published` with the workspace header; `social_drafts` alone under-reports live publishes because the mirror row is convention, not enforcement. Rationale in DECISIONS 2026-07-20. Fold into the AM Step 1B verification block and the PM awareness scan. **↻ 2026-07-22 — adopted and in force.** Used as the authoritative check this session: no new publishes since 07-15; breach ledger holds at 5. Keep it as the standing method; do not regress to `social_drafts`. **↻ 2026-08-15 — extended.** The published-posts API is necessary but not sufficient: it proves what *did* publish, not that a channel is *alive*. Verify a scheduled publishing task by its **output artifacts** — run-log file + Growth Log entry + Publer publish — because `enabled: true` with a fresh `lastRunAt` masked 4 consecutive silent `styer-gbp-weekly` fires. See DECISIONS 2026-08-15.
- \[x\] ~~Adam: build 3 Mailchimp Customer Journeys~~ — SUPERSEDED 2026-04-14. PA Welcome (6 emails) + DPA Guide (8 emails) moved to n8n+Resend workflows (`rwi3qEYgJKGGHkHc`, `0M8Vnf6MhB1xtaIg`), triggered from subscribe-lead.js. Mailchimp only handles list/tags. "Rate Watch" sequence not built — decide scope separately (name was ambiguous: market rate drops vs. quoted-rate watch for specific borrower).
- \[ \] NMLS# 513013 profile audit on all 4 social platforms
- \[ \] **n8n credential hygiene audit** (2026-04-22): while fixing `loanos-n8n-agent-secret`, found the Supabase `service_role` JWT hardcoded inline in all 3 HTTP nodes of workflow `nOCDV73m4M0jyL1B`. Pattern likely mirrored across the other \~16 workflows — any HTTP node hitting `uuqedsvjlkeszrbwzizl.supabase.co` that doesn't use a Supabase credential is a leak waiting to happen (key rotation = N workflow edits; any shared n8n JSON export leaks prod DB). Scope: (1) enumerate all HTTP nodes across all active workflows via MCP; (2) identify inline creds (Supabase JWT, Resend API key, Mailchimp API key, Publer API key, Anthropic API key); (3) migrate each to proper n8n credentials; (4) delete the inline values. Prereq: Adam creates the canonical credentials once in n8n UI (see `tasks/ADAM-TODO.md` \[SEC\] items).
- \[ \] Adam: review/approve Refi Watch email copy (Sequences A + D)
- \[ \] **NEEDS ADAM — Pause or redirect** `loanos-scenarios-am` **scheduled task. Recommendation (c) pause — PROVEN on production data, VERIFIED 4× (08-15, 08-17, 08-18, 08-19). ↻ 2026-08-19.** **🩺 08-19 — the last untested hypothesis is now closed: Scenarios is NOT broken, it is healthy. This does not change the answer; it removes an excuse and a risk.** Every prior run argued this line assuming the tool works. It was never checked. Scenarios last changed **2026-04-24**, and the ~4 months since shipped a command center, comp tracking, `loans.status_normalized`, org feature flags, an MS Graph adapter, and the `claude-fable-5` cutover — any of which could have silently broken a route nobody exercises. "He stopped using it because it stopped working" is a different problem pointing at a different decision, and the 08-15 memo's own caveats named only two readings (stopped needing it / went back to MC), both of which presume a working tool. **Tested three ways, all green.** (1) `npm run build` → **exit 0, zero errors**, `✓ 117/117` static pages; all **9** Scenarios API routes compiled (`calculate`/`save`/`generate-narrative`/`generate-pdf`/`generate-qa`/`backfill-qa`/`parse-statement`/`send-email`/`views`) plus `/dashboard/scenarios`, `/[id]`, `/new`, and borrower-facing `/share/[token]`. **Not a vacuous pass** — the tree's 8 dirty source files are all *untracked peer-agent components*, zero edits to any Scenarios file. (2) **Schema intact** — live `information_schema` returns **24 columns**, every Tier 2–8 addition present and correctly typed (`borrower_intent`, `borrower_qa`, `lo_note`, `narrative_edited`, `share_token`, `view_count`); all 4 tables the code touches resolve. (3) **Model string current** — the one runtime break a green build *cannot* catch, on the Tier 1 flagship feature: both AI routes import central `CLAUDE_MODEL` = `claude-fable-5`, and there are **zero** stale `claude-3*`/`sonnet-4*`/`opus-4*` literals in scenarios or share paths. **Why this matters to your decision:** (i) **"maybe he'd come back if it worked" is dead** — every remaining explanation for the silence is about **demand, not quality**, which is precisely the question this line says is yours to answer; (ii) **(c) pause carries no code-rot risk** — the fair objection that the cron is "at least watching for regressions" is now answered, it never actually did until today, and there are none, so pausing leaves nothing broken behind; (iii) **(b) redirect would build on sound code, not a repair job** — previously unknown. **⚠️ Limits, stated plainly: this is build/schema-level, NOT a runtime smoke test.** No scenario created, no calculation executed, no share page rendered, no PDF generated; calculation math / PDF layout / narrative quality untested since 04-24; a rotated env var (Anthropic/Resend), an RLS change, or storage-permission drift would all still pass a green build. Memo: `tasks/scenarios/audits/2026-08-19-scenarios-health-check.md`. Usage re-confirmed by the blessed query only, no re-audit: 34 rows, newest `2026-05-01`, **110 days silent**. **Next run: do NOT re-run the health check either** (build+schema+model green 08-19) — re-verify only if Scenarios code changes or a runtime error is actually reported. **🔧 08-18 — one thing changed, it cuts AGAINST the recommendation, so it leads: the "no clean landing zone for autonomous code" constraint recorded further down this line is RETIRED — it is false.** `git ls-remote --heads origin` shows **`fix/web-lead-dup-23505` @ `26d2601` live on `origin`**, pushed 2026-08-17 PM through a worktree cut off `origin/main` (`git worktree add <path> -b <branch> origin/main` → build → push → `git worktree remove --force`). Autonomous code CAN ship from this clone; only *merges to `main`* still need Adam. That constraint was true when recorded 07-21 and then **inherited unverified through ~15 runs** — and while it stood it made **(b) redirect look technically impossible**, which flattered (c). It was never a reason to pause. **Net: recommendation unchanged, but it now rests on ONE argument (zero usage) instead of two, and (b) is back on the table as shippable.** 08-18 usage re-confirmed with the single blessed query only, no re-audit: 34 rows, newest `2026-05-01`, `max(updated_at)` also `2026-05-01` — **109 days silent**. **🔴 The decisive measurement, first taken 08-15 after 4 months of arguing this line on process signals alone: Adam has not created a scenario in 108 days.** **↻ 08-17 live re-query (read-only): every figure below holds unchanged — 34 rows, newest `2026-05-01`, 0 created since, `borrower_intent`/`borrower_qa`/`lo_note`/`narrative_edited` all 0; the count is now 108 days. Two additions.** (i) **`max(updated_at)` anywhere in the table is also `2026-05-01`** — nothing edited, re-shared, or re-opened either. This closes the last live alternative reading ("maybe he edits old scenarios instead of creating new ones"). (ii) **Correction: the memo's "33 total share views" was wrong — the real figure is 22.** The memo's own monthly table (11+10+1) was always right, and live `sum(view_count)` = 22. Corrected in place in the memo with a dated note; **the conclusion is unaffected**, flagged only because Adam is being asked to decide from that document. **Next run must NOT re-audit** — measured 3× with identical results; `select count(*), max(created_at) from scenarios` suffices if a fresh signal is ever wanted. **Also found 08-17:** the 08-15 audit run wrote no CHANGELOG and no session-log entry (both bridged 08-17), and the **08-16 fire produced zero artifacts** (no SESSION_END written, so that run never reached its exit path; cause not determined and deliberately not guessed). ~~It also destroyed the uncommitted 07-05 → 08-15 status lines in `tasks/scenarios/subagent-status.md` by overwriting with `>` instead of `>>`.~~ **CORRECTED 08-18: that data-loss claim is unsupported — no lines were lost.** `subagent-status.md` is a **2-line rolling file by design** (SESSION_START + SESSION_END, overwritten each run): HEAD's committed copy ends 07-05 at exactly 2 lines, and the independent iCloud dupe `subagent-status 2.md` ends 07-17 at exactly 2 lines. `>` is what the task spec itself prescribes ("Write SESSION_START … to subagent-status.md"). Drop this from the concern list. **GOALS has now missed five straight Monday refresh windows** (07-27, 08-03, 08-10, 08-17; content frozen since 07-15) — `stat -L` still `Jul 20 11:19:21 2026`, `grep -in scenario` still line-72-only. **Original 08-15 finding, retained:** `scenarios` holds **34 rows total**, newest **2026-05-01** — 24 in March + 9 in April (5 of those 9 are self-tests, `borrower_name = "Adam Styer"`) + 1 in May, then nothing. **Every feature the program built in Tiers 2–8 has ZERO production usage:** `borrower_intent` 0 (the item the queue called "single highest-ROI"), `borrower_qa` 0, `lo_note` 0, 2-1 buydown 0 (`"buydownType": "none"` on all 23 purchase rows), down-payment-comparison 0, rent-vs-own 0, ARM-vs-fixed 0, cost-of-waiting 0, refi-timing 0. `scenario_type` holds only `purchase`/`refinance` — the two modes that predate the program. `narrative_edited` false on all 34. The last two real borrower scenarios (Daniel Barron 04-27, Farinaz Pisheh 05-01) **both skipped the AI narrative**, the Tier 1 flagship. Full memo with caveats: `tasks/scenarios/audits/2026-08-15-scenarios-usage-reckoning.md`. **This reframes the choice:** prior runs recommended (c) because the queue was empty; the stronger reason is that the program shipped 8 tiers across ~40 build sessions and production adoption of all of it is zero — the Mortgage Coach gap was never closed, the tool was abandoned. **(b) redirect** now carries a real prerequisite that is Adam's to answer, not an agent's to assume: *why did a feature-complete tool go unused?* Building "complicated income" templates into a tool with 106 days of zero engagement repeats the mistake at a new address. (a) retire stays off the table (Adam kept it on the Keep-running list at the 07-02 edit). Agent will NOT self-pause — GOALS line 72 keeps the cron and names no target, so the call is Adam's. **Also note: the "70 consecutive daily no-ops" framing is overstated** — per the 08-15 notebooklm-nightly finding (host sleep, not task defect), scenarios-am did not fire between 07-25 and 08-14; today is its first fire in 24 days. Streak counting is retired from this line as a decision input; the usage data supersedes it. Prior history: Scenarios program COMPLETE (Tiers 1–8, last code build 2026-04-24; queue empty). Since 2026-05-18 the cron has produced **70 consecutive no-build no-op exits** (through 2026-07-24; 07-19 + 07-20 + 07-23 gapped, gaps don't increment). **↻ 2026-07-22 AM:** GOALS `stat -L` **unchanged** at `Jul 20 11:19:21 2026`; `grep -in scenario` re-run anyway per the 07-21 method note → still only line 72. Nothing moved; recommendation **held, not re-argued** (07-24: `stat -L` still `Jul 20 11:19:21 2026`, grep still line-72-only, 07-23 gapped). Next natural refresh window = **Mon 2026-07-27** (3 days out — the decision point to watch). **Additional finding 07-22:** `tasks/scenarios/subagents/` contains only `00-notebooklm.md` — the Research / Builder / QA / Reporter playbooks that master-agent Sequences A–C invoke **do not exist on disk**, so a (b) redirect would also require authoring those four playbooks before any sequence could run. **Why the recommendation flipped:** the 07-18 run forecast **Mon 2026-07-20 as the next natural GOALS-refresh window and the decision point for this line**. That window arrived and passed — `stat -L` GOALS.md **ADVANCED to `Jul 20 11:19:21 2026`** (first mtime movement in 18 days), but `grep -in scenario` shows **no scenarios directive was added**; GOALS line 72 still just reads "scenarios-am (LO work — keep)". (Caveat: GOALS' footer still says `Last updated: 2026-07-15` and no content marker postdates 07-15, so the 07-20 touch may be metadata-only — either reading gives the same answer.) This is the **first declined redirect moment since the 07-02 un-pause**, and the one that was explicitly forecast. Until now the (b)-redirect recommendation rested on "no refresh window has occurred since the un-pause" — that premise is spent. **Context (unchanged):** the 07-02 edit RESUMED LoanOS product work (Command Center: dashboard / comp / reporting — **not** Scenarios), so "mission paused" is void; scenarios-am fires **un-paused-but-unassigned**. The master-agent charter binds this cron to Scenarios files only, so it cannot self-assign command-center work without scope-creeping into the loanos-autonomous lane. ~~**Independent hard constraint added 2026-07-21:** the clone is parked on `codex/website-ai-assistant`, **16 commits ahead of `origin/main`** — there is no clean place for autonomous code to land, so even a redirect cannot ship code until Adam returns the clone to main.~~ **RETIRED 2026-08-18 — FALSE.** The clone is still parked on codex (now 17 ahead, and it must stay untouched — Adam drives that branch), but that never blocked shipping: cut a worktree off `origin/main`, build there, push a feature branch, remove the worktree. Proven 08-17 PM (`fix/web-lead-dup-23505` @ `26d2601`, live on `origin`). A redirect CAN ship code; it just cannot merge to `main` without Adam. **Pick one:** **(c) pause the cron — RECOMMENDED** (one reversible line in the task config; ends the daily no-op; fully reversible if Scenarios work returns). **(b) redirect** — still viable if Adam wants the slot working; best-aligned target remains "complicated income" Scenarios templates (self-employed / 1099 / bank-statement / DSCR / jumbo) matching the positioning shift; alternates: daily refi-opportunity surfacing for past clients, overnight Scenarios PDF pre-warm. Note this has now been declined at every refresh window offered. (a) retire stays off the table (Adam kept it on the Keep-running list at the 07-02 edit). Agent will NOT self-pause or self-redirect: GOALS line 72 keeps the cron and names no target, so the call is Adam's. Flagged daily since 2026-04-25; refreshed in place per stale-flags rule, never re-stacked.
- \[ \] **NEEDS ADAM — `notebooklm` CLI auth expired — blocks THREE NotebookLM programs on one credential. 105 calendar days. DECISION NEEDED: re-auth or pause.** First detected 2026-05-03 PM; live-reprobed **2026-08-16 nightly** (and 08-14/15 before it), still `Authentication expired or invalid. Run 'notebooklm login'` (WebLiteSignIn redirect). **↻ 2026-08-16 — recovery is confirmed to be ONE action, not three:** `notebooklm --help` exposes a global `--storage PATH` flag defaulting to `~/.notebooklm/storage_state.json`, i.e. auth is nothing but that single Playwright storage-state file. A single successful `login` writes it and SEO/SEM + Lead Gen + Rancho all resume on their next fire — no per-notebook or per-task re-auth afterward. Still not agent-runnable (login opens a browser and waits on ENTER); this only bounds the human task. **↻ 2026-08-15 — scope corrected upward:** this is not just the two nightly halves. `~/.notebooklm/context.json` was written 08-14 16:07 pointing at the **Rancho** notebook (`f2e7baf6-…`), so `rancho-notebooklm-sync` (every 4h, enabled) is driving the same binary against the same dead session — ~6 more silent no-ops/day that never appear in the loanos logs. **One login clears SEO/SEM + Lead Gen + Rancho.** That makes (a) cheaper than it has looked for 80+ sessions. **ROOT CAUSE (diagnosed 2026-07-22, not previously established):** `~/.notebooklm/storage_state.json` mtime = **Mar 23 09:17**, 121 days old and never refreshed — the saved Google session simply aged out. `notebooklm login --help` confirms it "Opens a browser window for Google login. After logging in, press ENTER in the terminal." That is an interactive credential flow: **no scheduled agent can complete it**, and re-authenticating on Adam's behalf is out of scope. This is not a bug to fix — it is a 30-second human task. **Cost of inaction:** last successful PUSH+CURATE was 2026-05-02; ~80 nightly runs since have written nothing but "still blocked" log lines. Backlog on recovery: ~14 lead-gen artifacts + ~80 PM-side syncs; SEO/SEM notebook sits at its **50-source cap** with ~50 stale + ~30 ready-to-add, so the first good night needs a full rotation before any new pushes. **Pick one:** **(a)** run `/Users/adamstyer/.local/bin/notebooklm login` from a terminal, then `notebooklm list --json` should return JSON — next nightly picks up automatically; or **(b) pause `styer-notebooklm-nightly`** (and consider `rancho-notebooklm-sync` with it) until the knowledge base is worth maintaining again — 104 days of no-op runs is a fair argument for this, and it is fully reversible. Agent will not self-pause: GOALS keeps seo-sem-am/pm + lead-gen-am/pm on the keep-running list. **↻ 2026-08-15 — one prior sub-claim RETRACTED, do not re-raise:** the 07-22/07-23 runs reported a "logging gap — the cron fired ~22 days without writing a log." That was wrong. The 07-22/07-23 entries are still present uncommitted in the worktree 22 days on (HEAD stops at 06-24), proving writes are durable; the silences were **missed fires from host sleep**, independently corroborated by the 08-14 `loanos-autonomous` entry ("first fire in 21 days… did not fire 07-25→08-13") and by wall-clock jumping 22:12→23:32→02:21 CDT inside the 08-14 nightly session itself. The cron config is healthy and fires on target when the machine is awake — nothing to fix there. Refreshed in place per stale-flag rule; never re-stacked. **↻ 2026-08-19 — 108 days. Nothing changed in the outage; two things changed in what we KNOW about the recovery, and one of them is a correction to this very line.** **(i) CORRECTION — this line's own "SEO/SEM notebook sits at its 50-source cap with ~50 stale + ~30 ready-to-add" is NOT a measurement and must stop being cited as one.** `source list` requires the dead credential, so the figure is unverifiable today and traces back to the last successful sync, **2026-05-02 — 109 days ago**. It has been restated as fact here and in CONTEXT for ~80 sessions and it is load-bearing for the recovery plan. Now marked **ESTIMATED**. This is the same inherited-claim failure the 08-18 `scenarios-am` run retired at TODO L66 ("no clean landing zone for autonomous code" — carried unverified through ~15 runs, then proven false). Rotation-before-push stays the safe default; **verify the real source count on the first good night before acting on it.** **(ii) A forward risk was measured and CLOSED.** Step 5 of both playbooks re-adds `Styer_Growth_Log.md` as a source on recovery night. Measured this run: **1.37 MB / 17,086 lines / 201,934 words = ~40% of NotebookLM's ~500k-word per-source ceiling** → **`source add` will not fail on size**; it reaches the cap ~April 2027 at the observed ~1,200 words/day. No action needed, recorded so nobody re-raises it. **(iii) Backlog re-counted, not inherited: exactly 14 lead-gen artifacts (7 research + 7 specs) — and 13 of the 14 are from one 15-day burst, 05-01→05-15**, with only `2026-07-22-lead-scoring-model-v2.md` in the 96 days since. The queue is **frozen, not growing**; recovery is a single session, not a marathon. SEO/SEM has produced **zero** new research/specs since May. ⚠️ Several of those specs are **superseded** by the 05-17 site audit + 05-18 pile-realignment triage (L43 retires PR-2 outright) — **filter the push through the triage verdicts**, or recovery night seeds the notebook with retired funnel designs. **Net effect on the decision: (a) got cheaper and better-specified — the recovery night is now a known, bounded, one-session job.** Cron/host facts unchanged and not re-litigated (fired +73m late 23:21 CDT in a wake-catch-up burst 0.4s after `styer-site-daily`; ~10h host suspend inside the session; `rancho-notebooklm-sync` last fired 20:36 CDT 08-18 on the same dead credential). Refreshed in place per stale-flag rule; never re-stacked.
- \[ \] **NEEDS ADAM — Reconcile NotebookLM nightly playbook vs scheduled task email rule** (flagged 2026-04-26 PM; STILL OPEN as of 2026-05-01 PM, ~5 nightly runs since have honored the SKILL.md override): scheduled task `styer-notebooklm-nightly/SKILL.md` says "Do not send any emails to Adam. All reporting goes into project files only." But subagent playbooks `tasks/seo-sem/subagents/00-notebooklm.md` Step 5c + `tasks/lead-gen/subagents/00-notebooklm.md` Step 5c both say to send the digest via Zapier. Tonight (2026-05-01 PM) honored the SKILL.md override — both digests WRITTEN to `tasks/<area>/digests/2026-05-01-digest.md` only, NOT sent. Pick the rule and remove the contradiction: either (a) delete Step 5c from both playbooks (digests live in files only, no email), or (b) remove the "no emails" sentence from `styer-notebooklm-nightly/SKILL.md`. Until reconciled, future nightly runs will keep hitting this fork. **↻ 2026-07-22 — still open, still honoring the SKILL.md override** (digest skipped, not sent). Cheap to close and it will bite on the first recovery night: (a) is the smaller edit and matches what every run has actually done for ~80 sessions.
- \[ \] **NEEDS ADAM — Trim CONTEXT.md back under 150-line cap** (flagged 2026-05-03 Day 39 standup). Current size 161 lines (cap 150). Pre-existed before today's standup edit (content-neutral swap on Standup Agent Status block). The `Current Status` paragraph on line 20 is the bulkiest single block — months of cumulative session-status writes that mostly duplicate CHANGELOG entries already present. Recommended trim: archive everything in `Current Status` older than ~14 days to `CHANGELOG.md` (most of it is already there), keep only an active "what landed in the last two weeks" snippet. Per-agent status blocks (Social/Lead Gen/SEO-SEM/Scenarios/Standup) are within scope to keep; `What This Is` / `Repo` / `Rules For AI Sessions` are infrastructure. Risk if not done: agents that read CONTEXT.md continue to spend tokens on stale status carry-forward; cap creep accelerates. Surgery is content judgment, not safe in autonomous mode without Adam's call on what to keep.
- \[x\] **~~NEEDS ADAM — decide on n8n Drip Scheduler~~** `LqBb3YDLjS2eUrDE`**~~.~~** DECIDED 2026-04-16 PM: option (a) now, (c) later. Workflow archived via MCP (`active: false`, `isArchived: true`) — daily cron no longer runs, dead Outlook send node never fires again. Banner added to `/dashboard/drip-campaigns` so the UI stops pretending. Skipped option (b) Resend retarget (12 throwaway n8n nodes during a WDK cutover). The 6 campaigns (Ghost Referral, Incomplete App, Went Quiet, Long-Term Nurture, Past Client Retention, Realtor Relationships) move to Workflow DevKit as a dedicated phase AFTER Task 23 cutover completes — not as scope creep into Task 23. To restore: un-archive `LqBb3YDLjS2eUrDE` via MCP and re-activate.
- \[x\] **~~Populate~~** `automation_registry` **~~transactional templates — 2/7 done (2026-04-16 PM).~~** DONE 2026-04-16 PM (late-2). All 7 rows (Referral Intro, Review Request, Final CD, Pre-Approval, Contract Received, New Application Received, Refi Intake) now `email_mode='fixed_template'` with populated `email_template` + `email_variables`. Final CD TRID 3-day framing + wire-fraud warning preserved verbatim. See CHANGELOG 2026-04-16 PM (late-2) for per-row details and verification scan.
- \[ \] **Wire n8n code nodes to fetch templates from** `automation_registry` **at runtime.** Registry is now fully populated (body + subject + vars on all 8 rows). Per workflow: HTTP GET `automation_registry?source_id=eq.X&source_node_id=eq.Y`, substitute `{{var}}` tags on `email_template` + `subject_template`, send. Also swaps the dead Outlook draft node for Resend at the same time since we're editing the sender anyway. **Refi Intake caveat**: template uses `{{cash_label}}`, `{{cash_amt}}`, `{{escrow_row_html}}`, `{{processor_note}}` as pre-resolved derived vars — caller must compute conditionals (`cash_to_close` sign, `escrow === 'Waived'`, org = Adam) before substitution. **Pre-Approval caveat**: `{{lo_*}}` + `{{brand_header}}` + `{{calendly_link}}` require the same profiles/organizations/org_settings lookup the current JS does. **Contract Received caveat**: workflow now has 2 registry rows (`build-borrower-email` + `build-party-email` via `source_node_id`); both need lookups. **Decision before wiring**: do these workflows migrate to Workflow DevKit (matching web-lead-intake + nurture) instead of being rewired inside n8n? Blast radius: 6 production-active workflows.
- \[x\] `automation_registry` **~~schema gap — Contract Received Party Reply email.~~** DONE 2026-04-16 PM (late-3). Picked option (b): separate registry row `Contract Received — Party Reply` (id `68dc830e-3eec-44f4-ab24-05c71174964e`, source_node_id `build-party-email`). Unique index `(org_id, source_id, source_node_id) NULLS NOT DISTINCT` widened to support multi-email workflows. Existing row renamed `Contract Received — Borrower Welcome` (source_node_id `build-borrower-email`).
- \[x\] **~~n8n fix — Set Rate webhook Store Rate node:~~** DONE 2026-04-16 PM (late-3). Workflow `3iXImUkjgMitpJKt` repaired end-to-end: (1) removed `from_address` + `subject` from Store Rate body JSON (neither column exists); (2) fixed pre-existing Validate Rate bug — `return [{json}]` → `return {json}` for `runOnceForEachItem` mode. Verified via MCP `execute_workflow` (execution 5175 success) + new `activity_log` row `9b3a765d-d02d-4b04-997d-3a8e9bd23c2c` written clean. Manual rate updates can use `curl .../webhook/refi-watch-set-rate -d '{"rate":X}'` again. Rate Drop Alert threshold still 6.00%; today's 6.37% won't trigger alerts.

---

## Scott's Pilot — Multi-Tenant MVP (goal: first real second tenant)

> Uncle Scott Sears (org `40377391-6b4c-4d1a-81d2-ffd743876f0b`) — family pilot. Scott has no Arive (no LOS integration needed). Basic CRM only. When this ships cleanly, LoanOS has its first external tenant proof point. Honest scope reset 2026-04-19: May 1 deadline killed. No hard external commitment. Build what Scott actually needs, not what SaaS aspirations imagine. Drip infrastructure (campaigns, steps, suppressions, enrollments, sends) is already shipped and proven end-to-end (2026-04-19) — PA + DPA n8n workflows now write to Supabase. Scott's campaigns will reuse the same schema.

- \[ \] **Tenant scoping audit.** Walk every page/API route/server query — confirm `organization_id` / `org_id` filter applied. No exceptions. Required BEFORE Scott logs in. (Start here. Everything else depends on proving Scott's data can't leak into Adam's and vice versa.)
- \[ \] **RLS coverage verification.** `get_advisors` security pass + manual audit of every table. Log every policy; flag any table where a wrong-org user could read/write. **↻ 2026-08-19 (loanos-autonomous) — the `get_advisors` half RAN for the first time, and it found a live leak that is now FIXED in production.** 68 lints: **3 ERROR / 59 WARN / 5 INFO**. All three ERRORs were `rls_disabled_in_public` on **`rancho_chat_logs`, `n8n_run_logs`, `ai_node_logs`** — RLS off *and* full `DELETE,INSERT,SELECT,TRUNCATE,UPDATE` granted to `anon`, so the publishable key in client-side code could read or destroy them. **Proven live with a control**, not taken as a lint: anon read **137/137**, **23/23**, and all **1,713** rows respectively, while `activity_log` and `contacts` returned `*/0` under the same key. Worst of the three is `ai_node_logs` — 1,713 rows of the inbound-email classifier's `input_preview` + model `output`, i.e. **plaintext of the very content `activity_log_pii` encrypts** (migration 083). **Fixed**: migration `20260819120000_enable_rls_public_log_tables.sql`, commit `27f170d` on `fix/rls-public-log-tables`, applied to prod. Safe because all **60** n8n workflows were scanned and every node touching these tables uses an inline **`service_role`** JWT (bypasses RLS), with no app-code reader/writer. Post-fix: anon `*/0` on all three + `INSERT` 401; service role unchanged at 137/23/1713; **advisor ERROR count 3 → 0**. **Still open on this line: (a)** the manual per-table audit and policy log, **(b) 34 `SECURITY DEFINER` functions executable by `anon`** via `/rest/v1/rpc/` — including `get_my_organization_id`, `get_my_role`, `find_contact_by_email`, `find_duplicate_contacts`, `morning_briefing_pipeline` — which is the **most tenant-relevant remaining finding for Scott** and needs its own pass, **(c)** 24 functions with mutable `search_path`, **(d)** `auth_leaked_password_protection` disabled, **(e)** `fuzzystrmatch` sitting in `public` (the extension the 08-18 run disclosed installing; unused by shipped code — `drop extension if exists fuzzystrmatch;` still clean).
- \[x\] **~~Feature flags per-org.~~** DONE 2026-04-28 PM (commit `ec9659a`, Vercel `dpl_Bq1SAbkpJ4Ht1SnTaX4u8UzCgZaJ` READY). Migration 094 adds `organizations.features jsonb` (NULL = all-on, preserves Adam's UX). Scott's org seeded with 9 flags false. TopNav Email pillar + More menu (Scenarios/Lenders/Marketing/Drafts/Templates), dashboard `EmailAutomationCard`, contact-detail Drip card + Create Scenario, AutomationPanel all gate on flags. Admin UI at `/admin/feature-flags` (sys-admin only). RLS impersonation probe confirmed both default-on and locked-down paths.
- \[x\] **~~Manual Enrollment UI.~~** Contact detail DRIP CAMPAIGNS card always renders; `+ ENROLL` button opens inline campaign picker → POST `/api/drip/campaigns/[id]/enrollments`. DONE 2026-04-22 (commit `b3752fb`, Vercel READY).
- \[x\] **~~Drip end-to-end execution gap — prod deploy BLOCKED.~~** DONE 2026-04-24 (commit `96b7e93c`). Cron dropped to daily `0 13 * * *` (Hobby-plan compatible). All 7 backed-up commits now in production. CRON_SECRET still needs to be set in Vercel (see ADAM-TODO).
- \[x\] **~~Hold List UI.~~** DONE 2026-04-24 (commit `a1c2dec`). Settings page Hold List card: add form (email + reason), timestamped list with trash-icon delete. 3 API routes: GET/POST `/api/drip/suppressions`, DELETE `/api/drip/suppressions/[id]`. Org-scoped, no cross-tenant.
- \[x\] **~~Drip Dashboard widgets.~~** DONE 2026-04-27 PM (commit `a4e8f54`, Vercel `dpl_7SjND6PJmpHubZFV9TmTrpdTPEMF` READY). All three widgets shipped: active enrollments per campaign (CampaignCard), recent sends timeline (`RecentSendsTimeline` on `/dashboard/drip-campaigns`, 2026-04-26 PM `f54c16b`), completion rate per campaign (inline on CampaignCard, "X% completed", math = `completed / (completed + removed)`, tooltip exposes raw counts; empty state = "— completion" until enrollments finish). `DripCampaignWithStats` extended with `completed_count` + `removed_count` via two parallel `head:true` queries in `getCampaignsWithStats()`. No schema changes, no new endpoints.
- \[ \] **Drip cron RPC fix verification + end-to-end proof.** 2026-04-27 AM audit fixed `get_due_drip_enrollments()` (two non-existent columns: `ct.status` → `ct.stage`, `l.rate` → `l.interest_rate`). RPC now returns clean. Once `CRON_SECRET` is set in Vercel, manually enroll one Adam-controlled contact in PA Welcome → wait one cron tick → verify `drip_sends` row appears, `next_send_at` advances, RecentSendsTimeline populates. Audit: `tasks/lead-gen/audits/2026-04-27-drip-data-integrity-audit.md`.
- \[ \] **3 active campaigns missing authored content.** Long-Term Nurture (2 steps), Past Client Retention (6 steps), Realtor Relationships (4 steps already exist as `ef52ed56-8a22-4d15-9f12-a1796ccf17b6`). Apr 26 terminate-guard correctly silently kills any enrollment in these — meaning they look "active" but can't fire. Adam decides per campaign: archive (`UPDATE drip_campaigns SET status='archived'`) or author content. **2026-04-30 AM update:** Realtor Relationships copy is drafted at `tasks/lead-gen/drafts/2026-04-30-realtor-relationships-email-bodies.md` (4 voice-aligned bodies — Deal Anniversary / Milestone Celebration / Co-Marketing Offer / Holiday). Build now reduces further to: register UUID in `authored-emails.ts` + paste 4 bodies + verify 4 merge tags (`transaction_address`, `transaction_buyer_name`, `deal_count`, `first_deal_date`) resolve from `loans` table joined on `realtor_id` + n8n trigger wire (\~30 min post-criteria call).
- \[ \] **`/thank-you.html` cross-funnel post-submit audit findings (2026-05-05 AM).** 17 prioritized findings (5 HIGH / 6 MEDIUM / 6 LOW) at `tasks/lead-gen/research/2026-05-05-thank-you-page-audit.md`. 4th and final primary funnel-page audit. Single page handles all 6 routed `?type=` branches via inline IIFE at lines 621–720 plus 1 default fallback. **Compliance: 7 PASS / 1 N/A / 2 FAIL** — both addressable via PR-1. **HIGH:** H1 3-step "What Happens Next" Step 3 ("Letter or quote in 24 hrs") misleads rate-alert / FTB-DPA / lo-waitlist branches; H2 rate-alert branch hides Calendly entirely (kills path-to-call for warm leads); H3 FTB-DPA branch *replaces* phone CTA element (should append); H4 PA branch is bare (only h1 changes — no PA-specific reassurance copy despite warmest funnel); H5 default fallback for unknown/missing `?type=` is silent error mode (add dataLayer instrumentation). **MEDIUM:** M1 single `<title>` for all 6 branches; M2 redundant `referral_source` re-collection on quick-quote follow-up; M3 missing privacy reassurance on follow-up form; M4 generic Calendly h2; M5 `mailto:adam@thestyerteam.com` (lo-waitlist branch) violates Voice rule; M6 GA conversion fires for lo-waitlist (separate product). **LOW:** L1 inline `style=""` in 3-step block; L2 GA conversion no refresh-dedup; L3 no testimonial; L4 no meta description; L5 escape-literal em-dashes; L6 fixed Calendly height. **H5 deploy-gap from 05-04 closed inline this session** — `curl https://styermortgage.com/script.js?v=20260417` confirmed `lead_source` literals at L407/L523/L739; code IS deployed, Supabase delivery gap is upstream (real homepage submissions extremely rare ~1/wk). **Recommended ship order (5 PRs, ~80 min total):** PR-1 (~25 min, H1+H2+H3 inline-IIFE edit, closes 2 compliance FAILs); PR-2 (~15 min, H4 PA branch reassurance copy); PR-3 (~10 min, H5 dataLayer instrumentation); PR-4 (~15 min, cross-page brand-consistency + footer-address sweep across 4 pages); PR-5 (~15 min, all M+L items in light pass). **Audit series milestone**: combined HIGH-tier across 4 audits = 20 fixes; single "compliance closeout" PR bundling H1 from each audit (3-form TCPA two-checkbox split + thank-you 3-step block fix) would resolve 4 of 5 series compliance FAILs. Builder runs in `styerteam-mortgage-site` repo — Adam approves before any code change.
- \[ \] **`/get-preapproved.html` conversion audit findings (2026-05-01 AM).** 20 prioritized findings (5 HIGH / 7 MEDIUM / 6 LOW) at `tasks/lead-gen/research/2026-05-01-get-preapproved-conversion-audit.md`. PA funnel has 0 submissions in 9 days; 2026-04-28 diagnosis confirmed code path is clean → bottleneck is page CTR + form CTR. **HIGH (~45 min total ship time):** H1 headline rewrite (drops title's "24 hours / 40+ lenders" promise); H2 add optional purchase-price-range dropdown (highest-leverage form change); H3 named testimonials (replace "Austin Home Buyer"); H4 linkable review chip; H5 hero subhead anchor (rate or response-time). **MEDIUM:** M1 title cap; M2 meta CTA; M3 JSON-LD schema (coordinate w/ SEO/SEM); M4 og:image; M5 footer address (compliance — Texas SAFE Act / NMLS MU.4); M6 Loan Goal dropdown clarity; M7 close-rate footnote. **LOW:** 4th proof point, 60-sec microcopy, sticky mobile phone CTA, FAQ + FAQPage schema, "all 136 reviews" link, Lighthouse run. Compliance: 11/12 pass; M5 is the only flag. Recommended ship order in audit doc (4 PRs total). Builder runs in `styerteam-mortgage-site` repo (separate from loanos-clone) — Adam approves before any code change.
- \[ \] **Homepage forms (Quick Quote + Quick Contact) conversion + TCPA audit findings (2026-05-04 AM).** 17 prioritized findings (5 HIGH / 6 MEDIUM / 6 LOW) at `tasks/lead-gen/research/2026-05-04-homepage-forms-conversion-audit.md`. Two homepage forms: `#hero-quick-form` (Quick Quote, hero column 2) and `#quick-contact-form` (Quick Contact, mid-page). Homepage is currently the only owned-channel surface generating real captures (~1/wk steady-state, 8 'Website' contacts in 90 days vs 0 PA Funnel + 0 Rate Alert). **HIGH:** H1 TCPA two-checkbox split on BOTH homepage forms (closes BLOCKER-001 fully when bundled with rate-alert.html H1); H2 Quick Quote subhead missing (form floats without offer context); H3 generic CTA copy ("Get My Quote"); H4 Loan Goal options diverge across forms (5 vocabularies across 3 funnel pages — bundle with /get-preapproved M6); H5 `lead_source` body fields 'Quick Quote' / 'Quick Contact' not landing in DB (zero rows in 90 days while 8 'Website' fallback rows exist — likely Netlify deploy gap, verify by curl before next session). **MEDIUM:** M1 Quick Contact thank-you redirect symmetry; M2 purchase-price qualifier (cross-page bundle); M3 TCPA "loan" phrase (cross-page bundle); M4 trust badge dedup; M5 footer address (cross-page bundle); M6 JSON-LD MortgageBroker (cross-page bundle w/ SEO/SEM rotation). **LOW:** L1 btn-sm primary CTA; L2 form-input contrast on glass; L3 Quick Contact phone-call fallback; L4 novalidate JS-only; L5 0.75rem labels; L6 Quick Contact value casing. **Cross-page bundling — single 30-min PR closes all site-wide TCPA bundled-consent debt:** homepage 2 forms + rate-alert form = full BLOCKER-001 closure (get-preapproved already shipped). Builder runs in `styerteam-mortgage-site` repo — Adam approves before any code change.
- \[ \] **`/rate-alert.html` conversion audit findings (2026-05-02 AM).** 17 prioritized findings (5 HIGH / 6 MEDIUM / 6 LOW) at `tasks/lead-gen/research/2026-05-02-rate-alert-conversion-audit.md`. Rate Alert funnel has 0 subscribers in 34 days since deploy. **HIGH (~45 min total ship time):** H1 TCPA two-checkbox split (compliance + conversion — current single required checkbox bundles phone/email/text on a recurring-marketing product; mirror the get-preapproved pattern from BLOCKER-001); H2 subhead Lock-or-Wait differentiator (currently buries the actual product diff); H3 sharper CTA copy ("Send Me Friday's Rate Update →" vs current "Get My Weekly Rate Updates →"); H4 form-column social proof (currently zero in form column); H5 sample email rates undated/rate-agnostic (currently 5-week-old "Week of March 28" preview — kills credibility, also eliminates ongoing maintenance). **MEDIUM:** M1 email-typo prevention (Mailcheck or inline 30-line); M2 form-tagline frequency anchor; M3 rate-move-alert language softened to match what's actually built (Refi Watch unbuilt/blocked); M4 close-rate footnote (carry-over with get-preapproved); M5 og:image (carry-over); M6 JSON-LD schema (coordinate w/ SEO/SEM rotation). **LOW:** L1 sample From: address (`thestyerteam.com` violates global CLAUDE.md rule), L2 stat-label consistency, L3 cross-sell to /get-preapproved, L4 sample-label microcopy, L5 FAQ + FAQPage, L6 mobile form-card stacking. **Cross-page bundling:** 4 items overlap with get-preapproved findings (OG image / 21-day footnote / footer address / JSON-LD) — should ship as single shared cross-page PR rather than per-page work. Builder runs in `styerteam-mortgage-site` repo — Adam approves before any code change.
- \[ \] **Basic templated email send.** One-off send from contact detail → pick template from `automation_registry` → substitute vars → send via Resend → log to `activity_log`. Not drip, just one-shot. \~1 day.
- \[ \] **Scott onboarding flow.** Manual first: Adam creates Scott's user + org membership, imports his contacts via CSV, walks Scott through UI in a live session. (Scott's contacts + closed loans already imported — confirm 2026-04-19.)
- \[ \] **Post-onboarding iteration.** Whatever Scott tries and can't figure out becomes the first real-user feedback. Fix top 3 confusions before adding any new features.
- \[x\] **~~MISMO 3.4 importer.~~** DONE 2026-04-23. `/api/mismo/import` creates contact + loan from a Calyx Point MISMO XML export in one call; `ImportMismoButton` on `/dashboard/loans` (drag-drop modal, a11y complete). Shared parser in `src/lib/mismo/parse.ts`. Scott uploads his Point export → gets a loan in LoanOS without LOS integration. Follow-ups below (non-blocking, log before multi-borrower files):
  - Contact match race: two simultaneous uploads of same borrower insert two contact rows under the same org. Fix = catch Postgres `23505` on contact insert and re-fetch, or add unique index on `(organization_id, lower(email))`. (Low frequency; caught by reviewer, not urgent for Scott's solo use.)
  - \[x\] ~~Loan dedup skipped when `loan_number` is null (pre-submission Calyx exports).~~ DONE 2026-04-28 PM autonomous. Secondary dedup branch added in `src/app/api/mismo/import/route.ts` — when `loan_number` is absent and both `property_address` + `loan_amount` are populated, matches against existing `(org_id, contact_id, property_address, loan_amount)` rows.
  - Parser regex is greedy — in co-borrower files, first `<FirstName>` / `<ContactPointEmailValue>` wins regardless of `BorrowerRoleType`. Fine for single-borrower beta with Scott, must fix before any multi-borrower file lands.
  - \[x\] ~~`MISMOUpload.tsx` (scenarios page, pre-existing) swallows server error body~~. DONE 2026-04-28 PM autonomous. Now reads the JSON `error` field from non-OK responses (with HTTP-status fallback) and surfaces the real reason in the modal.

> Anti-scope: don't build Arive sync, email classifier, AI drafting, or auto-enrollment until Scott says he wants it. These are for Adam's ops, not Scott's pilot.

---

## Refi Opportunity List (V2 — after Scott V1 drip is verified)

> Decided 2026-04-28 (Adam): no auto-enrollment on rate triggers. Rate sheets are too nuanced (LTV tiers, loan size, product) to act on a single global threshold. Instead, surface refi candidates as a list, alert Adam, let him decide enrollment + customization per contact. Retires the n8n Refi Watch Sequences A/B/C/D + the Set Rate webhook entirely.

- \[ \] **Schema:** `organizations.refi_threshold_rate numeric` (default 6.5), `contacts.refi_dismissed_until timestamptz` (snooze).
- \[ \] **Query helper** `getRefiOpportunities(orgId)` — past clients (loan status closed/funded) where `loans.interest_rate >= organizations.refi_threshold_rate`, not opted out, not on hold list, not dismissed (or dismissed_until passed). Returns: contact, current rate, loan amount, LTV (if known), years since closing, last contact date.
- \[ \] **Page** `/dashboard/refi-opportunities` (or tab on Contacts) — sortable table. Row actions: Enroll in Refi Opportunity drip / Open contact (for one-off email) / Dismiss 90 days.
- \[ \] **TopNav badge** — count of current opportunities. Glance-and-click. (Skip weekly digest email — gets ignored.)
- \[ \] **Settings** — input for `refi_threshold_rate` (per-org, default 6.5).
- \[ \] **New campaign:** Refi Opportunity Watch — 3-4 authored emails (PA-style structure, refi-specific copy). Manual enroll only.
- \[ \] **Retire n8n Refi Watch:** archive Sequences A/B/C/D + Set Rate webhook (`3iXImUkjgMitpJKt`) + related schedules. Document in DECISIONS.md.

> Per-org by design — Scott sets his own threshold, sees his own past clients (he has past clients in LoanOS already per 2026-04-28).

---

## Self-Serve Tenant Domain Onboarding (post-V1 scaling)

> Decided 2026-04-28 (Adam): manual Resend domain verification for first 1-2 tenants is fine; build self-serve before tenant #3+. Skipping shared-sending-domain option (`@send.loanos.app`) — every LO wants their real domain on outbound.

- \[ \] **Settings page section** — "Email Sending Domain" card. Inputs: domain. Submit button.
- \[ \] **API:** `POST /api/email/domains` → calls Resend `POST /domains` with the LO's domain. Stores Resend `domain_id` on `org_settings`. Returns the DNS records (SPF + 2 DKIM) for the UI to render.
- \[ \] **UI:** displays each DNS record with type/name/value + a Copy button. "Verify" button polls `GET /api/email/domains/[id]/verify`.
- \[ \] **API:** `GET /api/email/domains/[id]/verify` → calls Resend `GET /domains/{id}` → returns `{ status: 'pending'|'verified'|'failed' }`. On verified, sets `org_settings.from_email = {lo_email}@{domain}` (or lets LO pick the local-part) and shows green check.
- \[ \] **Migration:** add `org_settings.resend_domain_id text`, `org_settings.resend_domain_status text`.
- \[ \] **Admin override:** sys-admin page lists all org domains + statuses; can re-trigger verify or delete on tenant offboarding.

> Eliminates the manual Resend-dashboard step. Tenant goes from "I want my own domain" → sending in ~10 min (DNS propagation), zero ops touch.

## Next (after Now items clear)

- \[ \] Security finding #5 (field-level encryption) — ADAM-BLOCKED (GLBA attorney consultation required)
- \[x\] ~~Security finding #9 (admin action log)~~ — DONE 2026-04-16. Migration 088 + `logAdminAction()` + 3 routes wired.
- \[x\] ~~Security finding #10 (sys vs org admin)~~ — DONE 2026-04-16. `requireOrgAdmin()` added to `src/lib/admin/auth.ts`.
- \[x\] ~~PII Phase 4: drop plaintext columns from~~ `activity_log` — DONE 2026-04-12. Migration 083 applied, post-check passed.
- \[ \] Renovation Phase 3: Contacts — merged timeline, referral tracking, realtor performance
- \[ \] Marketing site: demo user with synthetic data (blocks May 1 launch screenshots)
- \[ \] Activate LO Waitlist n8n workflow after copy review
- \[ \] Create Mailchimp audience "LoanOS Waitlist"
- \[ \] **Convert Publish to Netlify background function** — rename `generate-newsletter.js` → `generate-newsletter-background.js` (and same for `generate-rate-update`). Background functions return 202 in &lt;1s and run up to 15 min, eliminating the Vercel 60s timeout risk on publish. Preview stays synchronous. Client needs to poll/webhook for completion — decide pattern during implementation. Context: done via proxy 2026-04-14 (commit 0759bea), but 40-60s publish workload is one bad Anthropic latency spike away from timing out.

## Backlog (someday/maybe)

- \[ \] Renovation Phase 4: multi-tenant UI — onboarding wizard, "Connect Arive" flow
- \[ \] A-6: consolidate \~30 service-role routes onto `createUserScopedClient()` helper
- \[ \] A-11: move agent routes under `/api/webhooks/agents/[org_slug]/...`
- \[ \] CSP nonce rollout (drop `unsafe-inline` from script-src)
- \[ \] hCaptcha on [styermortgage.com](http://styermortgage.com) web-lead form
- \[ \] SOC 2 Type 1 kickoff
- \[ \] Film Reels: Posts 44, 51, 23, 16, 18
- \[ \] Create Canva assets: Posts 1, 5, 6, 8, 10, 12, 13, 22, 27, 43, 47, 52, 55, 60
- \[x\] Weeks 1-3 social posts rebuilt in social_drafts (21 posts inserted 2026-04-08)
