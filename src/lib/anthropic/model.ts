/**
 * Single source of truth for the Claude model string used across all API routes.
 * Update here when upgrading models — no need to touch individual route files.
 */
export const CLAUDE_MODEL = 'claude-fable-5'
