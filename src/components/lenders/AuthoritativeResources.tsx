import { guidelineSourceVersions } from '@/lib/documents/authoritative-sources';
export default function AuthoritativeResources() { return <details className="mb-6 rounded-xl border border-border p-4"><summary className="cursor-pointer font-semibold">Authoritative guidelines and source versions</summary><p className="mt-3 text-sm text-muted-foreground">Publisher references checked September 5, 2026. Publication dates are recorded separately from effective dates. Verify the applicable section, later updates and lender overlays for each file.</p><div className="mt-4 grid gap-3 md:grid-cols-2">{guidelineSourceVersions.map(s => <article key={s.id} className="rounded-lg border border-border p-4"><p className="text-xs text-muted-foreground">{s.publisher}</p><h2 className="font-semibold"><a className="underline" href={s.url} target="_blank" rel="noreferrer">{s.title} ↗</a></h2><p className="mt-1 text-sm">{s.edition}</p><p className="mt-2 text-sm text-muted-foreground">{s.citation}</p><p className="mt-2 text-xs text-muted-foreground">Source observation: {s.observedAt} · {s.id}</p>{'archiveUrl' in s && <a className="text-sm underline" href={s.archiveUrl} target="_blank" rel="noreferrer">Prior published versions ↗</a>}{'verificationUrl' in s && <a className="text-sm underline" href={s.verificationUrl} target="_blank" rel="noreferrer">Publisher verification ↗</a>}</article>)}</div></details>; }
