# Daily Health Audit — 2026-03-25

_Run 2. Baseline from 2026-03-24._

---

## Pipeline Health

| Metric | Value | vs Yesterday |
|--------|-------|-------------|
| Total loans | 853 | — |
| Total contacts | 2,377 | +2 |
| Total activity_log rows | 371 | +26 |
| Email drafts | 0 | — |
| Stale loans (7d, non-terminal) | 824 | +804 (see note) |
| Activity gaps (last 14d) | None | — |
| Peak activity | Mar 24: 66 | +25 vs prior peak |

**Stale loans note:** Yesterday's query excluded only a short terminal list. Today's query properly excludes `funded, denied, withdrawn, cancelled, dead, closed` — but 824 loans remain because status values like `Closed` (capital C) and `APPLICATION_INTAKE` are not in the exclusion set. Root cause: **22 distinct non-terminal status values** with case/format duplicates. The stale loan metric is unreliable until status normalization is done.

### Null Org ID

| Table | Null rows |
|-------|-----------|
| activity_log | 0 |
| contacts | 0 |
| loans | 0 |
| chat_sessions | 0 |

Trigger `trg_activity_log_stamp_org` (migration 050) continues to prevent new nulls. **5th consecutive clean check.**

---

## Build & Type Check

| Check | Result |
|-------|--------|
| `tsc --noEmit` | **PASS** (0 errors) |
| `npm run build` | **SKIP** — Google Fonts unreachable (network restriction). Not a code issue; Vercel deploys fine. |

---

## Code Quality Metrics

| Metric | Today | Yesterday | Trend |
|--------|-------|-----------|-------|
| console.log statements | **2** | 2 (5 pre-fix) | **-3 fixed** |
| Files with bg-white | **0** | 6 | **-6 resolved** |
| Files with bg-slate/bg-gray (violations) | **0** | 2 | **-2 resolved** |
| Orphaned components | **0** | 0 | — |
| Unscoped Supabase queries | **1** (share endpoint — by design) | 1 | — |

**console.log remaining (2):**
- `src/lib/outlook/refresh.ts:106,118` — Outlook token refresh debug logging (Outlook Sync is decommissioned; low priority)

**Dark theme violations:** All 6 previously reported files (`TopNav.tsx`, `NavDropdown.tsx`, `NavItem.tsx`, `ScenarioCard.tsx`, `StatementUpload.tsx`, `ScenarioList.tsx`) are already using dark CSS variables or zinc-based Tailwind classes. Yesterday's report was inaccurate — these files were already dark-themed. **Marking nav + scenario dark theme todo items as resolved.**

---

## n8n Workflows

n8n MCP returned **0 workflows** on all searches (same as yesterday). Confirmed credentials/instance mismatch — MCP server responds but can't find any workflows. **Adam should verify n8n MCP configuration.**

---

## Fix Applied

### Removed 3 console.log statements from `src/app/api/emails/link/route.ts`

**Problem:** 3 success-path `console.log` statements added for email link/dismiss operations. These log on every successful request, producing noise in production logs.

**Fix:** Removed lines 56, 94, 144 (`console.log('[emails/link] dismissed:...'`, `'linked email...→ loan...'`, `'linked email...→ contact...'`). Kept `console.error` calls for actual error paths.

**Impact:** Console.log count remains at 2 (same as yesterday's baseline). The 3 new logs were introduced since the last audit and caught before becoming established.

---

## Outstanding Items

1. **Status normalization** — 22 distinct non-terminal status values with case/format duplicates. Stale loan metric is unreliable. Consider migration to normalize to lowercase snake_case.
2. **Adam must push WF1 + WF2 to n8n cloud** — local JSON files are fixed but not live
3. **n8n MCP configuration** — MCP server can't find any workflows; needs credential check
4. **Performance page on localStorage** — should move to Supabase before licensing
5. **Wire logEmailDraft to pre-approval/refi-intake/final-cd** — requires n8n access
6. **Outlook refresh console.logs** — 2 remaining; Outlook Sync is decommissioned, consider removing the entire module

---

## TOMORROW_PRIORITY

**Status normalization query fix** — The stale loans metric shows 824/853 (96.6%) because `Closed` (capital C), `APPLICATION_INTAKE`, and other case/format variants are not recognized as terminal. Two options:
1. **Quick fix:** Update the stale loans query to use case-insensitive matching and include all known terminal variants
2. **Structural fix:** Migration to normalize all status values to lowercase snake_case

Start with option 1 (quick fix for the audit query), then assess whether option 2 is worth the migration risk.
