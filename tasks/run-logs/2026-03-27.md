# LoanOS Daily Report — 2026-03-27

## ACTION REQUIRED (for Adam)

- **20+ stale loans stuck in "Started"** — all last updated 2026-03-17, no activity log history. Appear to be a bulk import that was never processed. Recommend reviewing and marking dead/cancelled if they're not active files. These will continue polluting pipeline health checks until resolved.
- **J9Pe24vUi6fpZtdZ (Pre-Approval Lead Notify)** — still inactive. Needs Mailchimp credential configured in n8n UI before it can be activated.

## WATCH ITEMS

- n8n `eJG4wckrj6SmSpm1` (Weekly Testimonial Social Post) and `AK1fBcaX1cPcdlGx` (Review Request Email) both show as **active** in n8n — per MEMORY they were intentionally inactive. Verify this is intentional and update MEMORY if so.

## ALL CLEAR

- Build: PASS
- TypeScript: PASS (0 errors)
- Null org_id rows: 0 across activity_log, contacts, loans
- Email drafts queue: empty (no stuck pending drafts)
- Dark theme violations: 0 (all 6 files from 2026-03-24 baseline already fixed)
- Orphaned components: 0 true orphans (orphan check was corrected — NavDropdown/NavItem/GlobalSearch/SmartActionQueue all have valid consumers)
- n8n workflows: 15 active, 1 flagged inactive (J9Pe24vUi6fpZtdZ — expected per MEMORY)

## BUILD

Status: **Pass**
Error count: 0 (yesterday: 0 — trend: stable)

## CODE QUALITY METRICS

- console.log in API routes: **0** (yesterday: 4 — fixed today)
- Orphaned components: **0** (yesterday: N/A — grep scope corrected)
- Dark theme violations: **0** (yesterday: 0 — stable, all previously fixed)
- Untyped `any` casts: not scanned today
- Unscoped Supabase queries: not scanned today

## PIPELINE HEALTH

- Stale loans (3+ days): **20+** (all "Started"/"On Hold"/"Suspended", last updated 2026-03-17 — bulk import batch, needs Adam's review)
- Pending email drafts (24h+): **0**
- Activity gaps (5+ days): **10** (subset of stale loans above — all have zero activity log history)
- n8n workflow errors: not checked via execution logs (n8n MCP search shows no errored workflows)

## FIXED_TODAY

- `src/app/api/emails/link/route.ts` — removed 3 debug `console.log` statements (dismiss confirmation, link-to-loan, link-to-contact). `console.error` calls preserved for error paths.
- `src/app/api/agents/milestone/route.ts` — removed 1 debug `console.log` for opt-out skip. Logic unchanged (skips when `borrowerOptedOut = true`).
- Verification: `grep -rn "console.log" src/app/api/` returns 0 matches. Build passes.

## RECURRING_ISSUES

- **Stale "Started" loans from bulk import (2026-03-17)** — first seen this run. 20+ loans with no activity, stuck in non-terminal status. Not a code bug — data governance issue. Adam should resolve.

## TOMORROW_PRIORITY

**Confirm n8n status for `eJG4wckrj6SmSpm1` and `AK1fBcaX1cPcdlGx`** — both show active in live n8n but MEMORY marks them intentionally inactive. Update MEMORY.md to match actual live status. Then: scan untyped `any` casts and unscoped Supabase query patterns that weren't covered today.

## LESSONS_LEARNED

- **TOMORROW_PRIORITY may already be resolved** — yesterday's priority (dark theme nav violations) was already done when today's run checked. Always verify the current state before assuming the fix is still needed. The metrics will tell you.
- **Orphan check must search all of `src/`, not just `src/app/`** — components in `src/components/` import other components. Searching only `src/app/` produces false orphan positives. Corrected command: `grep -rl "$name" src/ --include="*.tsx"`.
