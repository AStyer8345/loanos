# PM Web Research — 2026-03-31

## Session Context
Focus: Netlify Functions async/await + environment variable patterns (BLOCKER-004/005 context) + Week 4 FTB planning

---

## Source 1: Netlify Functions Environment Variables Docs
**URL:** https://docs.netlify.com/build/functions/environment-variables/
**Relevance:** BLOCKER-004 — `LOANOS_URL` must be set via Netlify UI (not netlify.toml) for runtime access

Key findings:
- Environment variables set via Netlify UI are available as `process.env.VARIABLE_NAME` in standard serverless functions
- **Critical note:** Variables in `netlify.toml` are NOT available to functions at runtime — must use Netlify UI/API
- Scope must include "Functions" when setting env var in Netlify UI — check scope when adding `LOANOS_URL`
- Changes require a new deploy to take effect after setting
- March 2026 edge case: user-defined env vars reportedly missing in *scheduled/background* functions — NOT an issue for standard on-demand functions like `subscribe-lead.js`

**Implication for Adam:** When adding `LOANOS_URL` in Netlify dashboard, confirm scope includes "Functions" before saving. Then trigger redeploy (git push).

---

## Source 2: Netlify Functions Overview
**URL:** https://docs.netlify.com/build/functions/overview/
**Relevance:** BLOCKER-005 — confirms `await` requirement for async calls in serverless context

Key findings:
- Netlify Functions have a default timeout of 10 seconds for synchronous functions
- Background tasks that don't complete before function returns are killed — confirms BLOCKER-005 root cause
- `context.waitUntil()` is available for truly fire-and-forget tasks (logging, analytics) without blocking response
- For critical data writes (like CRM creation), must `await` inline — not fire-and-forget

**Implication:** BLOCKER-005 fix (`await notifyPreApprovalLead(...)`) is the correct approach for CRM writes that must succeed.

---

## Source 3: Scotsman Guide — Opportunity Opens for First-Time Buyers
**URL:** https://www.scotsmanguide.com/residential/opportunity-opens-for-firsttime-buyers/
**Relevance:** Week 4 planning — FTB funnel content strategy

Key findings:
- 2026 FTB market expanding due to modest rate stabilization (~6.8%) and new DPA programs
- Brokers who build educational tools now will own the "nurture to close" cycle
- Key FTB pain points: down payment myths, credit score confusion, "am I ready?" anxiety
- Recommended touchpoints: timeline calculator, down payment tool, step-by-step guide

**Implication for Week 4:** FTB Guide should focus on "down payment myths" as top lead magnet angle — high search volume, high FTB anxiety.

---

*Generated by Lead Gen PM Agent — 2026-03-31*
